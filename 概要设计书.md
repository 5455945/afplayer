### 🎵 个人音乐内录与元数据同步系统 - 需求与设计文档

**文档版本**: v2.5（基于需求规格说明书 v1.2 同步更新）
**适用场景**: 仅限个人开发、个人使用的本地音乐备份与播放工具
**变更摘要**: 新增存储空间监控、搜索筛选、批量管理、播放列表、重复检测、诊断报告导出、音频路由监听模块设计；新增 playlists/playlist_tracks 数据模型；新增数据库迁移备份策略和 WAL checkpoint 策略；补充封面 EXIF 清理、通知隐私模式、Doze 模式处理、录制冲突通知设计

---

### 1. 需求概述

#### 1.1 项目背景
开发者希望构建一个私有Android应用，能够在QQ音乐/华为音乐等主流平台播放音乐时，自动将音频流以MP3格式录制到本App私有目录，并同步抓取歌曲元数据（标题、艺人、专辑、封面等），最终在本App内形成可离线播放的个人音乐库。

#### 1.2 核心功能需求

| 模块 | 功能点 | 优先级 | 说明 |
| :--- | :--- | :--- | :--- |
| 音频捕获 | 系统内部音频内录 | P0 | 仅捕获媒体音频流，不录入麦克风环境音 |
| 元数据同步 | 实时获取播放信息 | P0 | 标题、艺人、专辑、封面、时长 |
| 状态联动 | 播放状态自动控制录音 | P0 | 播放→开始录；暂停/切歌→停止并保存 |
| 文件处理 | MP3编码与ID3标签写入 | P0 | 录音完成后自动嵌入元数据到文件标签 |
| 本地管理 | 私有存储与索引数据库 | P1 | 文件存于App私有目录，SQLite维护映射关系 |
| 本地播放 | 内置播放器读取本地库 | P1 | 支持列表播放、元数据显示、封面展示 |
| 后台保活 | 前台服务持久化 | P0 | 防止系统杀后台导致录音中断 |
| 错误诊断 | 异常日志与恢复 | P1 | 关键操作日志、失败可追溯、服务可恢复 |
| 质量标记 | 异常静音检测与标记 | P1 | 检测录制中的异常静音片段，标记可疑曲目供人工审核 |

#### 1.3 非功能性需求
- **隐私隔离**: 所有文件存储在 `/data/data/{packageName}/` 下，不被系统媒体扫描器索引
- **权限最小化**: 仅需 `RECORD_AUDIO`、通知使用权、前台服务权限，无需外部存储读写权限
- **容错性**: 元数据缺失或录音异常时不崩溃，生成无标签MP3并标记为待补全；关键异常写入 `error_logs` 表供诊断
- **可恢复性**: 前台服务被系统杀死后，能通过 `recording_sessions` 表恢复或安全清理进行中的录音
- **数据一致性**: 多表写入使用事务；删除曲目时级联清理文件和数据库记录
- **合规边界**: 不涉及任何逆向工程、DRM破解或网络分发功能

---

### 2. 系统架构设计

#### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                     你的 APK (UI层)                       │
│  ┌──────────┐  ┌──────────────┐  ┌───────────────────┐  │
│  │ 本地曲库播放器 │  │  录制状态面板  │  │  设置/授权引导     │  │
│  └─────┬────┘  └──────┬───────┘  └───────────────────┘  │
│        │              │                                  │
│  ┌─────▼──────────────▼──────────────────────────────┐   │
│  │              ViewModel / Repository                │   │
│  │           (Room DB + File I/O + MediaPlayer)       │   │
│  └───────────────────────┬───────────────────────────┘   │
├──────────────────────────┼──────────────────────────────┤
│                    前台服务层 (Core)                       │
│  ┌───────────────────────▼───────────────────────────┐   │
│  │             RecordingOrchestrator                  │   │
│  │                                                    │   │
│  │  ┌────────────────┐   ┌────────────────────────┐   │   │
│  │  │ SessionArbiter │──►│ MetadataSessionListener │   │   │
│  │  │ (会话仲裁)      │   │ (MediaController)      │   │   │
│  │  └────────────────┘   └───────────┬────────────┘   │   │
│  │                                    │                │   │
│  │  ┌─────────────┐   ┌──────────┐    │                │   │
│  │  │AudioCapture │──►│LAME      │◄───┘                │   │
│  │  │(PCM采集)    │   │Encoder   │                     │   │
│  │  └─────────────┘   │(NDK/JNI) │                     │   │
│  │                    └────┬─────┘                     │   │
│  │                         │                           │   │
│  │                  ┌──────▼──────┐                    │   │
│  │                  │ID3 Tag Writer│                    │   │
│  │                  └─────────────┘                    │   │
│  └────────────────────────┬────────────────────────────┘   │
└───────────────────────────┼──────────────────────────────┘
                            │
                 ┌──────────▼──────────┐
                 │  App Private Storage │
                 │  /files/music/*.mp3  │
                 │  /databases/music.db │
                 └──────────────────────┘
```

#### 2.2 核心模块职责

| 模块 | 技术选型 | 职责描述 | 对应需求 |
| :--- | :--- | :--- | :--- |
| NotificationListenerService | `NotificationListenerService` | 接收系统通知变更，提取其他 App 的 MediaSession Token，是获取外部 MediaSession 的唯一合法入口 | FR-02, FR-03 |
| MetadataSessionListener | `MediaSessionManager` + `MediaController` | 监听**白名单应用**的MediaSession，回调metadata和playbackState变更 | FR-02, FR-03 |
| SessionArbiter | Kotlin + Room | 多 MediaSession 并发时的会话仲裁，按优先级选择唯一 active session | FR-11, FR-23 |
| TargetAppManager | Room (`target_apps` 表) + SharedPreferences | Room 存储白名单列表数据；SP 存储全局配置（锁定 session、状态机参数、功能开关） | FR-11 |
| ProjectionManager | `MediaProjectionManager` | 引导用户完成屏幕录制授权（MediaProjection），管理投影实例的生命周期（获取/缓存/失效重授权） | FR-13 |
| AudioCapture | `AudioPlaybackCapture` (API 29+) | 创建AudioRecord实例，以44.1kHz/16bit/Stereo采集**白名单应用**的媒体音频PCM流；内嵌静音检测，驱动 qualityFlag 计算 | FR-01, FR-03, FR-10 |
| RecordingOrchestrator | Kotlin Coroutines + StateFlow | 协调音频流与元数据的时序对齐，管理录音生命周期状态机；元数据缺失时降级为纯录音模式（tagStatus=3） | FR-03, FR-08, FR-12, FR-17 |
| LAME Encoder | libmp3lame via JNI | 将PCM管道式编码为CBR/VBR MP3，避免内存溢出 | FR-04 |
| ID3 Tag Writer | mp3agic / jaudiotagger | 录音结束后写入ID3v2.4标签（含封面二进制数据）；标签编辑后自动回写 | FR-05, FR-24 |
| 本地存储 | Room + File API | 维护全部 7 张表；结构化 error_logs 写入与分级保留清理 | FR-06, FR-09, FR-14 |
| 文件导出模块 | Storage Access Framework (SAF) | 通过系统文件选择器将曲库中的 MP3 复制到公共目录，无需申请外部存储权限 | FR-16 |
| 本地播放器 | MediaPlayer / ExoPlayer + MediaSessionCompat | 本地曲库播放、后台播放、通知栏播放控制、耳机拔出检测；曲目质量标记展示与审核 | FR-07, FR-07a, FR-15 |
| 通知管理器 | NotificationManager + NotificationCompat | 前台服务持续通知（显示录制状态与歌曲信息）、播放控制通知、录制冲突切换通知、存储空间预警通知。支持隐私模式（隐藏歌曲名） | FR-08, FR-23, FR-29 |
| 存储空间监控器 | StatFs + SharedPreferences | 录制前预估空间是否充足；实时监控剩余空间，500MB/100MB 两级预警；触发通知和页面内横幅 | FR-22, FR-26 |
| 搜索与筛选引擎 | Room Query + Kotlin Flow | 支持按标题/艺人/专辑关键字实时搜索，按来源App/质量标记/时间范围组合筛选 | FR-18, FR-19 |
| 播放列表管理器 | Room (playlists + playlist_tracks 表) | 创建/编辑/删除自定义播放列表；曲目与列表的多对多关联管理 | FR-25 |
| 重复检测器 | Room Query（tracks 表 identityHash 比对） | 在 PRE_RECORDING 阶段比对 identityHash 与曲库已有记录；录制完成后若重复则提醒用户 | FR-28 |
| 诊断报告导出器 | SAF + 文件打包（ZIP） | 一键导出 error_logs(全量) + session 记录(最近50条) + 脱敏配置 + 设备信息为 ZIP 文件 | FR-27 |
| 音频路由监听器 | AudioManager + ACTION_AUDIO_BECOMING_NOISY | 监听蓝牙/有线耳机的连接和断开；向 Orchestrator 报告路由切换事件 | FR-30 |

#### 2.3 目标应用白名单机制

`AudioPlaybackCapture` 依赖目标 App 的音频输出——如果目标 App 没有音频焦点（被其他 App 打断、暂停等），它的音频不会进入系统混合器，自然录不到。因此采用 **"白名单 + 音频焦点"双重过滤** 策略：

```
┌────────────────────────────────────────────────────────────┐
│                   TargetAppManager (目标应用管理器)           │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  1. 白名单存储 (target_apps 表)                             │
│     - packageName: App 包名                                │
│     - displayName: 用户可读名称                            │
│     - priority: 优先级 (10的倍数，方便插入)                  │
│     - enabled: 是否启用                                    │
│     - addedAt: 添加时间                                    │
│                                                            │
│  2. 默认白名单 (首次安装预置)                                │
│     - com.tencent.qqmusic (QQ音乐) priority=10             │
│     - com.netease.cloudmusic (网易云音乐) priority=20       │
│     - com.kugou.android (酷狗音乐) priority=30             │
│     - com.kuwo.player (酷我音乐) priority=40               │
│     - com.ximalaya.ting.android (喜马拉雅) priority=50     │
│                                                            │
│  3. 用户可自定义                                            │
│     - 添加/移除白名单应用                                   │
│     - 调整优先级顺序（10的倍数，可在中间插入）               │
│     - 临时禁用某个应用（不删除）                            │
│                                                            │
│  4. 全局配置 (SharedPreferences)                            │
│     - lockedSessionPackage: 用户手动锁定的 session         │
│     - minRecordingDurationMs: 最短录音阈值（默认30000）     │
│     - preRecordingTimeoutMs: PRE_RECORDING超时（默认5000）  │
│     - silenceDetectionEnabled: 是否启用静音检测（默认true） │
│                                                            │
│  5. 运行时查询接口                                          │
│     - getEnabledApps(): List<TargetApp>                    │
│     - isTargetApp(packageName: String): Boolean            │
│     - getPriority(packageName: String): Int                │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**录音触发条件（必须同时满足）**：

| 条件 | 说明 | 检测方式 |
|:---|:---|:---|
| **白名单匹配** | 播放音频的 App 必须在白名单中且 `enabled=true` | `TargetAppManager.isTargetApp(packageName)` |
| **音频焦点持有** | 该 App 当前持有音频焦点，音频正在输出到系统混合器 | `MediaSession` 状态为 `STATE_PLAYING` 且 `active=true` |
| **元数据可用** | 该 App 的 MediaSession 已推送 metadata（至少 title 或 artist 非空） | `TrackMetadata.title != null \|\| artist != null` |

**AudioPlaybackCapture 配置示例**：

```kotlin
// AudioCapture.start() 内部实现
// projection 由 ProjectionManager 提供，来自 MediaProjectionManager.createScreenCaptureIntent() 的用户授权结果
// 若 projection 为 null（用户未授权或授权已失效），直接返回 Result.failure
val targetUids = targetAppManager.getEnabledApps().map { app ->
    packageManager.getApplicationInfo(app.packageName, 0).uid
}

val projection = projectionManager.getActiveProjection()
    ?: return Result.failure(AppException("MediaProjection not available"))

val config = AudioPlaybackCapture.Builder(projection)
    .addMatchingUsage(AudioAttributes.USAGE_MEDIA)  // 仅捕获媒体音频
    .apply {
        targetUids.forEach { uid -> addMatchingUid(uid) }  // 仅捕获白名单 App
    }
    .build()

// AudioRecord 缓冲区大小：getMinBufferSize(44100, STEREO, PCM_16BIT) * 4
// 4倍最小缓冲可平衡音频延迟与系统调度稳定性，必须显式指定避免 underrun
val audioRecord = AudioRecord.Builder()
    .setAudioPlaybackCaptureConfig(config)
    .setAudioFormat(...)
    .setBufferSizeInBytes(bufferSize)
    .build()
```

**关键设计决策**：
- **不在白名单的 App 播放音频** → 不录制，状态机保持 IDLE
- **白名单 App 暂停/失去焦点** → MediaSession 状态变为 PAUSED，触发 FINALIZING
- **白名单 App A 播放中，非白名单 App B 抢焦点** → App A 的 MediaSession 仍为 PLAYING，但音频被系统混合器静音，录到的是静音数据

**音频焦点丢失检测（双重机制）**：

由于 `AudioManager.OnAudioFocusChangeListener` 只能监听自己 App 的焦点变化，无法直接知道其他 App 的焦点状态，因此采用 **MediaSession 状态为主 + 静音检测兜底** 的双重机制：

```
┌─────────────────────────────────────────────────────────────┐
│              音频焦点丢失检测（双重机制）                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  方案 B（主）：MediaSession 状态监听                          │
│  ─────────────────────────────────────────                  │
│  • 监听白名单 App 的 MediaSession 状态变化                   │
│  • 当状态从 STATE_PLAYING 变为 STATE_PAUSED/STATE_STOPPED    │
│    时，视为该 App 失去音频焦点或被暂停                        │
│  • 触发 RecordingOrchestrator.finalizeCurrentSession()      │
│  • 优点：准确、即时、无延迟                                  │
│  • 缺点：依赖目标 App 正确实现 MediaSession                   │
│                                                             │
│  方案 A（兜底）：静音检测                                     │
│  ─────────────────────────────────                          │
│  • AudioCapture 持续监测 PCM 数据的音量                      │
│  • 通过统一的 onSilencePhaseChanged() 回调报告静音状态       │
│  • 连续 5s 静音 (THRESHOLD 阶段) 视为焦点丢失               │
│  • 优点：能捕获 MediaSession 未正确更新的边界情况            │
│  • 缺点：有 5 秒延迟、可能误判（歌曲本身的静音段落）          │
│                                                             │
│  协同策略：                                                  │
│  ─────────                                                  │
│  • 任一方案触发即执行 FINALIZING                             │
│  • 方案 B 优先：MediaSession 状态变化立即触发                │
│  • 方案 A 兜底：MediaSession 状态未变但持续静音时触发        │
│  • 防误判：如果静音期间收到新 metadata（切歌），重置静音计时  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**异常静音片段检测与标记**：

歌曲中可能存在非音乐本身的静音片段（如广告插播、系统提示音、音频焦点被抢占等），这些片段会导致录制的 MP3 质量下降。需要检测并标记这些异常静音，方便用户后续人工审核。

```
┌─────────────────────────────────────────────────────────────┐
│              异常静音片段检测机制                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  检测规则：                                                  │
│  ─────────                                                  │
│  • 静音阈值：SILENCE_AMPLITUDE_THRESHOLD (采样值 < 100)     │
│  • 短静音阈值：SHORT_SILENCE_MS (500ms ~ 3s)                │
│  • 长静音阈值：LONG_SILENCE_MS (> 3s)                       │
│  • 歌曲结尾保护：录音最后 3s 内的静音不标记为异常            │
│                                                             │
│  分类策略：                                                  │
│  ─────────                                                  │
│  1. 短静音 (500ms ~ 3s)：                                   │
│     - 可能是歌曲本身的间奏静音（正常）                       │
│     - 也可能是系统提示音（异常）                             │
│     - 标记为 SHORT_SUSPECT（可疑），不自动判定为异常         │
│                                                             │
│  2. 长静音 (> 3s)：                                          │
│     - 几乎不可能是歌曲本身的静音段落                         │
│     - 大概率是广告插播、音频焦点被抢占等异常情况             │
│     - 标记为 LONG_ABNORMAL（异常）                           │
│                                                             │
│  3. 静音期间特征分析：                                       │
│     - 如果静音期间检测到非零采样值（如系统提示音），          │
│       标记为 INTERFERENCE（干扰）                            │
│     - 如果静音期间完全为零，标记为 SILENCE（纯静音）          │
│                                                             │
│  4. 焦点丢失升级规则：                                       │
│     - 当静音达到 5s 触发 FINALIZING 时，                     │
│       该录音期间所有 SHORT_SUSPECT 片段自动升级为            │
│       LONG_ABNORMAL（因为它们实际是焦点丢失的前兆）          │
│                                                             │
│  记录内容：                                                  │
│  ─────────                                                  │
│  • startTimeMs: 静音开始时间（相对于录音开始）              │
│  • endTimeMs: 静音结束时间                                   │
│  • durationMs: 静音持续时长                                  │
│  • type: SHORT_SUSPECT / LONG_ABNORMAL / INTERFERENCE       │
│  • peakAmplitude: 静音期间的最大采样值（用于判断是否有干扰） │
│                                                             │
│  曲目质量标记：                                              │
│  ────────────────                                            │
│  • qualityFlag: 整首曲目的质量标记                          │
│    - GOOD: 无异常静音片段                                    │
│    - SUSPECT: 有短静音片段（可能是歌曲本身）                 │
│    - ABNORMAL: 有长静音片段或干扰（大概率录制异常）          │
│  • abnormalSilenceCount: 异常静音片段数量                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 2.4 层间接口契约

以下 Kotlin interface 定义了各模块之间的通信协议，是层间解耦的核心。

**接口约定总览**：
- **返回值风格**：跨层调用的 `suspend` 函数统一返回 `Result<T>`；纯查询接口允许返回可空类型。
- **线程契约**：回调接口标注执行线程（如 `@WorkerThread`），禁止在回调中直接操作 UI 或阻塞 I/O。
- **幂等性**：`stop()` / `abort()` / `clearTags()` 等销毁类操作保证幂等，多次调用无副作用。
- **进程模型**：当前为**单进程架构**（UI 与 Service 同进程），Bound Service 直接通过 Binder 获取实例调用。若未来改为多进程，需额外设计 AIDL + Coroutine 桥接。

##### 2.4.1 UI ↔ RecordingService

UI 层通过 **Bound Service + StateFlow** 与服务通信，不使用 BroadcastReceiver（避免状态丢失）。

```kotlin
class RecordingService : Service() {
    // 服务对外暴露的状态流，UI 通过 ViewModel observe
    val recordingState: StateFlow<RecordingSessionState>

    // UI 主动控制接口（通过 Binder 获取 Service 实例后调用）
    // 注意：startCapture() 无参，实际录制的 targetPackage 由 SessionArbiter 自动决定
    // UI 仅控制"开始/停止"，不指定具体 App
    // 说明：本系统不提供手动 pause/resume，录音启停完全由 MediaSession 的 PLAY/PAUSE 事件自动驱动
    suspend fun startCapture(): Result<Unit>
    suspend fun stopCapture(): Result<TrackRecord>
}

// UI 层统一的状态模型
sealed class RecordingSessionState {
    object Idle : RecordingSessionState()
    data class PreRecording(val targetPackage: String) : RecordingSessionState()
    data class Recording(
        val targetPackage: String,
        val currentMetadata: TrackMetadata?,
        val durationMs: Long,
        val bufferLevel: Float  // 0.0~1.0，用于 UI 水位指示
    ) : RecordingSessionState()
    data class Finalizing(val reason: FinalizeReason) : RecordingSessionState()
    data class Error(val error: AppException) : RecordingSessionState()
}

// FINALIZING 的触发原因
enum class FinalizeReason {
    PAUSED,             // MediaSession 状态变为 PAUSED
    SKIP_NEXT,          // 切歌事件
    NEW_METADATA,       // 收到新 metadata（切歌）
    SILENCE_THRESHOLD,  // 静音达到 5s（焦点丢失兜底）
    AUDIO_INTERRUPTED,  // AudioCapture 异常中断
    SESSION_SWITCH,     // SessionArbiter 切换到更高优先级 session
    EMERGENCY           // 不可恢复异常触发的紧急保存
}
```

##### 2.4.2 AudioCapture → RecordingOrchestrator

```kotlin
interface AudioCapture {
    // 初始化 AudioRecord，返回是否成功
    // 内部调用 audioRecord.startRecording()，异步准备，超时 5s 未收到数据视为失败
    // 参数说明：targetPackage 仅用于日志记录，实际捕获所有白名单 App 的混合音频
    //          因为 AudioPlaybackCapture 的 UID 过滤在创建时一次性设置，无法动态切换
    suspend fun start(targetPackage: String): Result<Unit>

    // 注册 PCM 数据回调
    // 警告：所有回调均在 AudioRecord 内部读取线程执行（@WorkerThread），禁止直接操作 UI 或阻塞 I/O
    fun setPcmDataListener(listener: PcmDataListener)

    // 停止并释放资源；幂等操作，多次调用无副作用
    // 返回 Result.failure 时，异常已内部记录到 error_logs，调用方无需额外处理
    suspend fun stop(): Result<Unit>

    // 当前采集状态（内存状态，非持久化）
    val isActive: Boolean
}

interface PcmDataListener {
    // pcmData: 16bit/Stereo PCM 字节数组
    // timestampNs: 数据采集时的 System.nanoTime()
    // 线程：AudioRecord 内部读取线程（@WorkerThread）
    fun onPcmData(pcmData: ByteArray, timestampNs: Long)

    // AudioRecord 异常中断（焦点抢占、目标App被杀等）
    // 线程：AudioRecord 内部读取线程（@WorkerThread）
    fun onCaptureInterrupted(error: AudioCaptureException)

    // 静音阶段变化（统一回调，覆盖焦点丢失兜底 + 异常片段标记两个场景）
    //
    // phase 生命周期：
    //   START(500ms) → ONGOING(每1s) → THRESHOLD(5s)
    //
    // Orchestrator 处理逻辑：
    //   START     → 开始记录异常静音片段（AbnormalSilenceRecord）
    //   ONGOING   → 更新片段的 endTimeMs 和 peakAmplitude
    //   THRESHOLD → 触发 FINALIZING（焦点丢失兜底）
    //              → 将本次录音所有 SHORT_SUSPECT 片段升级为 LONG_ABNORMAL
    //
    // 防误判：如果静音期间收到新 metadata（切歌），Orchestrator 重置静音计时器，
    //         不会收到 ONGOING 和 THRESHOLD 回调
    //
    // 线程：AudioCapture 内部检测线程（@WorkerThread）
    fun onSilencePhaseChanged(phase: SilencePhase, durationMs: Long, peakAmplitude: Int)
}

enum class SilencePhase {
    START,       // 检测到 500ms 静音，开始记录异常片段
    ONGOING,     // 静音持续中，每 1s 回调一次，更新 peakAmplitude
    THRESHOLD    // 静音达到 5s，触发 FINALIZING
}
```

##### 2.4.3 MetadataSessionListener → RecordingOrchestrator

```kotlin
interface MetadataSessionListener {
    // 启动监听；autoConnect=true 时自动连接当前活跃的 MediaSession
    suspend fun startListening(autoConnect: Boolean = true)

    fun setCallback(callback: MetadataCallback)

    suspend fun stopListening()
}

interface MetadataCallback {
    // 元数据变更；metadata 中的字段可能为 null（部分App不填全）
    // 线程：MediaSessionManager 回调线程（Binder 线程池），禁止直接操作 UI
    fun onMetadataChanged(sessionToken: MediaSessionCompat.Token, metadata: TrackMetadata)

    // 播放状态变更
    // 线程：MediaSessionManager 回调线程（Binder 线程池），禁止直接操作 UI
    fun onPlaybackStateChanged(
        sessionToken: MediaSessionCompat.Token,
        state: PlaybackStateCompat,
        activePackage: String
    )

    // 注意：session 切换事件由 SessionArbiterListener 统一报告
    // MetadataCallback 不再提供 onActiveSessionChanged，避免职责重叠
    // SessionArbiter 是 session 切换的 source of truth

    // 所有目标 session 消失
    // 线程：MediaSessionManager 回调线程（Binder 线程池）
    fun onAllSessionsLost()
}

// 统一元数据模型，屏蔽 MediaMetadataCompat 的 key-value 访问
// 注意：不持有 Bitmap 对象，避免 OOM 和序列化问题；封面通过 coverPath 本地路径引用
data class TrackMetadata(
    val title: String?,
    val artist: String?,
    val album: String?,
    val durationMs: Long?,            // 来自 MediaMetadata.METADATA_KEY_DURATION
    val coverUri: Uri?,               // 原始 Content URI，可能过期（仅用于临时获取）
    val coverPath: String?,           // 本地封面缓存路径（优先使用，避免 URI 过期问题）
    val sourcePackage: String,
    val isAdvertisement: Boolean = false
) {
    // 用于判断"是否是新歌"的指纹：基于 title+artist+album 的组合哈希
    // 空字段使用占位符避免全空冲突；加入 sourcePackage 降低跨应用碰撞概率
    fun identityHash(): String {
        val raw = listOf(
            title ?: "_NULL_",
            artist ?: "_NULL_",
            album ?: "_NULL_",
            sourcePackage
        ).joinToString("|")
        return raw.sha256().substring(0, 16)
    }
}
```

##### 2.4.4 SessionArbiter（会话仲裁器）

```kotlin
interface SessionArbiter {
    // 注册一个活跃的 session
    // 线程：MediaSessionManager 回调线程（Binder 线程池），内部线程安全
    fun registerSession(sessionToken: MediaSessionCompat.Token, packageName: String)

    // 注销一个 session
    // 线程：MediaSessionManager 回调线程（Binder 线程池），内部线程安全
    fun unregisterSession(sessionToken: MediaSessionCompat.Token)

    // 获取当前应录制的 active session（优先级最高且正在 PLAYING 的）
    // 线程：任意线程，内部线程安全
    fun getActiveSession(): MediaSessionCompat.Token?

    // 当 active session 变化时通知 Orchestrator
    // 线程：UI 线程（初始化时调用），内部线程安全
    fun setListener(listener: SessionArbiterListener)
}

// 目标应用管理器接口（层间契约补充）
interface TargetAppManager {
    // 获取所有已启用的白名单应用
    fun getEnabledApps(): List<TargetApp>

    // 判断指定包名是否在白名单中且已启用
    fun isTargetApp(packageName: String): Boolean

    // 获取指定包名的优先级（未找到返回 Int.MAX_VALUE）
    fun getPriority(packageName: String): Int

    // 更新应用启用状态
    suspend fun setEnabled(packageName: String, enabled: Boolean): Result<Unit>

    // 调整优先级顺序
    suspend fun updatePriority(packageName: String, priority: Int): Result<Unit>
}

interface SessionArbiterListener {
    // active session 切换
    fun onActiveSessionChanged(
        oldToken: MediaSessionCompat.Token?,
        newToken: MediaSessionCompat.Token
    )
}

// 仲裁规则：
// 1. 用户手动锁定的 session 始终优先（lockedSessionPackage 配置）
// 2. 未锁定时，按 target_apps 表的 priority 字段排序
// 3. 同优先级时，先注册的优先
// 4. active session 进入 PAUSED/STOPPED 时，自动切换到下一个
```

##### 2.4.5 RecordingOrchestrator → LAME Encoder

```kotlin
interface LameEncoder {
    // 初始化编码器；sampleRate 通常为 44100，bitrate 单位 kbps
    suspend fun initialize(
        sampleRate: Int = 44100,
        channels: Int = 2,
        bitrate: Int = 192,
        outputPath: String
    ): Result<Unit>

    // 写入 PCM 数据；内部使用环形缓冲区，非阻塞
    // 返回当前缓冲区水位 (0.0~1.0)
    //
    // 三级水位线：
    //   < 0.7   → 正常
    //   0.7~0.9 → WARNING 日志，Orchestrator 应降速写入
    //   0.9~1.0 → ERROR 日志，丢弃最旧的 PCM 数据（可能产生音频跳跃）
    //   = 1.0   → 抛出 EncoderBufferOverflowException
    //
    // 失败策略：返回 Result.failure 后，encoder 内部状态已损坏，不可继续使用，
    // 必须调用 abort() 终止并重建 encoder
    suspend fun writePcm(data: ByteArray): Result<Float>

    // flush 剩余数据并关闭文件；调用后 encoder 不可复用
    // 返回实际写入的字节数和时长
    suspend fun flushAndClose(): Result<EncodeResult>

    // 强制中断（异常路径）；可能产生不完整的 MP3
    // 幂等操作；返回失败时异常已内部记录
    suspend fun abort(): Result<Unit>
}

data class EncodeResult(
    val outputPath: String,
    val fileSizeBytes: Long,
    val durationMs: Long
)
```

##### 2.4.6 ID3 Tag Writer

```kotlin
interface Id3TagWriter {
    // 向已完成的 MP3 文件写入 ID3v2.4 标签
    // coverData 可选；写入失败不影响 MP3 音频数据
    // coverMimeType 优先根据 coverData 文件头魔数自动检测（JPEG/PNG），调用方可覆盖
    // coverData 中的 EXIF 元数据（GPS、设备信息等）在写入标签前已由封面缓存模块清除
    // 封面缩放：最大尺寸 500x500，保持宽高比，质量 85%，格式 JPEG
    suspend fun writeTags(
        mp3Path: String,
        metadata: TrackMetadata,
        coverData: ByteArray? = null,
        coverMimeType: String? = null
    ): Result<Unit>

    // 清除 MP3 文件中已有的 ID3 标签（用于重试写入前的清理）
    // 写入失败时返回 Result.failure，但不会损坏音频数据
    suspend fun clearTags(mp3Path: String): Result<Unit>
}

// 封面缓存模块中的 EXIF 清理逻辑（发生在封面本地缓存时）
interface CoverCacheManager {
    // 从 Content URI 获取封面数据并缓存到本地
    // 内部执行：
    //   1. ContentResolver.openInputStream(uri) 读取原始数据
    //   2. BitmapFactory.decodeStream() → Bitmap
    //   3. ExifInterface 读取 EXIF → 若有 GPS/设备信息则标记需要清理
    //   4. 缩放至 500x500（保持宽高比）
    //   5. Bitmap.compress(JPEG, 85) → 此时 EXIF 已被清除（新 Bitmap 不含 EXIF）
    //   6. 写入到 covers/{identityHash}.jpg
    // 步骤 5 的重新编码自然清除了 EXIF，无需额外处理。
    // 若未来切换到直接复制原始文件的方案，需显式调用 ExifInterface 清除。
    suspend fun cacheCover(uri: Uri, identityHash: String): Result<String>
}
```

##### 2.4.7 Repository → Room DAO

```kotlin
@Dao
interface TrackDao {
    @Insert(onConflict = OnConflictStrategy.ABORT)
    suspend fun insertTrack(track: TrackRecord): Long

    @Update
    suspend fun updateTrack(track: TrackRecord)

    @Query("SELECT * FROM tracks ORDER BY recordedAt DESC")
    fun observeAll(): Flow<List<TrackRecord>>

    @Query("SELECT * FROM tracks WHERE id = :id")
    suspend fun getById(id: Long): TrackRecord?

    @Query("SELECT * FROM tracks WHERE filePath = :path")
    suspend fun getByPath(path: String): TrackRecord?

    // 查询标签写入失败的曲目（需要用户手动重试）
    @Query("SELECT * FROM tracks WHERE tagStatus IN (2, 3) ORDER BY recordedAt DESC")
    fun observeFailedTags(): Flow<List<TrackRecord>>

    // 查询正在处理中的曲目（tagStatus=0，标签尚未写入完成）
    @Query("SELECT * FROM tracks WHERE tagStatus = 0 ORDER BY recordedAt DESC")
    fun observePendingTags(): Flow<List<TrackRecord>>

    // 查询质量异常的曲目（供用户审核）
    @Query("SELECT * FROM tracks WHERE qualityFlag != 'GOOD' ORDER BY recordedAt DESC")
    fun observeQualityIssues(): Flow<List<TrackRecord>>

    // 查询引用指定封面路径的曲目数量（用于级联删除时判断是否可以删除封面文件）
    @Query("SELECT COUNT(*) FROM tracks WHERE coverPath = :coverPath")
    suspend fun countByCoverPath(coverPath: String): Int

    // 删除单个曲目记录（级联删除由 Repository 层协调）
    @Query("DELETE FROM tracks WHERE id = :trackId")
    suspend fun deleteTrack(trackId: Long)

    // 更新曲目删除状态（用于级联删除的补偿机制）
    @Query("UPDATE tracks SET deleteStatus = :status WHERE id = :trackId")
    suspend fun updateDeleteStatus(trackId: Long, status: Int)

    // 查询指定删除状态的曲目（补偿机制重试用）
    @Query("SELECT * FROM tracks WHERE deleteStatus = :status")
    suspend fun getByDeleteStatus(status: Int): List<TrackRecord>
}

@Dao
interface RecordingSessionDao {
    @Insert
    suspend fun insertSession(session: RecordingSessionRecord): Long

    @Update
    suspend fun updateSession(session: RecordingSessionRecord)

    @Query("SELECT * FROM recording_sessions WHERE status = 'RECORDING' OR status = 'PRE_RECORDING'")
    suspend fun getActiveSessions(): List<RecordingSessionRecord>

    @Query("DELETE FROM recording_sessions WHERE id = :id")
    suspend fun deleteSession(id: Long)
}

@Dao
interface ErrorLogDao {
    @Insert
    suspend fun insert(log: ErrorLogRecord): Long

    @Query("SELECT * FROM error_logs ORDER BY timestamp DESC LIMIT :limit")
    suspend fun getRecent(limit: Int = 100): List<ErrorLogRecord>

    // 按严重级别筛选
    @Query("SELECT * FROM error_logs WHERE severity = :severity ORDER BY timestamp DESC LIMIT :limit")
    suspend fun getBySeverity(severity: String, limit: Int = 100): List<ErrorLogRecord>

    // 30 天自动清理（时间维度）
    @Query("DELETE FROM error_logs WHERE timestamp < :before")
    suspend fun pruneByTime(before: Long)

    // 分级保留：删除超出上限的 WARNING/INFO 日志（保留最新 500 条）
    @Query("""
        DELETE FROM error_logs WHERE id IN (
            SELECT id FROM error_logs 
            WHERE severity IN ('WARNING', 'INFO')
            ORDER BY timestamp DESC
            LIMIT -1 OFFSET 500
        )
    """)
    suspend fun pruneBySeverity()
}

@Dao
interface AbnormalSilenceDao {
    @Insert
    suspend fun insert(silence: AbnormalSilenceRecord): Long

    @Query("SELECT * FROM abnormal_silences WHERE trackId = :trackId ORDER BY startTimeMs ASC")
    suspend fun getByTrackId(trackId: Long): List<AbnormalSilenceRecord>

    @Query("SELECT COUNT(*) FROM abnormal_silences WHERE trackId = :trackId")
    suspend fun countByTrackId(trackId: Long): Int

    @Query("SELECT type FROM abnormal_silences WHERE trackId = :trackId")
    suspend fun getTypesByTrackId(trackId: Long): List<String>

    @Query("DELETE FROM abnormal_silences WHERE trackId = :trackId")
    suspend fun deleteByTrackId(trackId: Long)

    // 批量升级：将 SHORT_SUSPECT 升级为 LONG_ABNORMAL（焦点丢失时）
    @Query("UPDATE abnormal_silences SET type = 'LONG_ABNORMAL' WHERE trackId = :trackId AND type = 'SHORT_SUSPECT'")
    suspend fun upgradeSuspectToAbnormal(trackId: Long)
}

@Dao
interface TargetAppDao {
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insert(app: TargetApp)

    @Update
    suspend fun update(app: TargetApp)

    @Query("SELECT * FROM target_apps WHERE enabled = 1 ORDER BY priority ASC")
    suspend fun getEnabledApps(): List<TargetApp>

    @Query("SELECT * FROM target_apps WHERE packageName = :packageName")
    suspend fun getByPackage(packageName: String): TargetApp?

    @Query("SELECT EXISTS(SELECT 1 FROM target_apps WHERE packageName = :packageName AND enabled = 1)")
    suspend fun isAppEnabled(packageName: String): Boolean

    @Query("UPDATE target_apps SET enabled = :enabled WHERE packageName = :packageName")
    suspend fun setEnabled(packageName: String, enabled: Boolean)

    @Query("UPDATE target_apps SET lastRecordedAt = :timestamp WHERE packageName = :packageName")
    suspend fun updateLastRecordedAt(packageName: String, timestamp: Long)

    @Query("DELETE FROM target_apps WHERE packageName = :packageName")
    suspend fun delete(packageName: String)
}
```

#### 2.5 文件导出模块设计

**导出流程**：

```
[用户在曲库列表/详情页点击"导出"] → [调用系统文件选择器(SAF)选择目标目录]
    → [用户确认目录] → [系统将 MP3 文件复制到所选目录]
    → [导出成功通知用户] → [可选：是否从本地曲库删除（默认否）]
```

**接口定义**：

```kotlin
interface ExportManager {
    // 启动导出流程；通过 ActivityResultLauncher 调用 SAF 的 ACTION_OPEN_DOCUMENT_TREE
    // 用户选择目录后，系统将 MP3 复制到目标目录
    // 返回复制后的文件 URI，供 UI 展示或分享
    suspend fun exportTrack(trackId: Long, launcher: ActivityResultLauncher<Intent>): Result<Uri>

    // 复制文件的核心逻辑；在 IO 线程执行
    // 复制过程中监听磁盘空间，不足时提前终止
    suspend fun copyToPublicDirectory(sourcePath: String, targetTreeUri: Uri): Result<String>
}
```

**关键设计决策**：
- **无需外部存储权限**：完全依赖 SAF（Storage Access Framework），通过 `ACTION_OPEN_DOCUMENT_TREE` 获取用户授权的目录持久化权限（`takePersistableUriPermission`）。
- **保留完整标签**：导出的 MP3 直接复制源文件，ID3 标签和封面数据保持原样。
- **磁盘空间预检**：复制前通过 `StatFs` 检查目标目录所在分区剩余空间，若不足则提前返回 `Result.failure`。
- **取消处理**：用户在文件选择器中点击取消 → 不做任何操作，不记录错误日志。
- **重复导出**：同一曲目可多次导出到不同目录，系统不做去重限制。

**错误处理**：

| 错误场景 | 检测方式 | 处理策略 | 用户感知 |
|:---|:---|:---|:---|
| 目标目录空间不足 | `StatFs` 预检 | 提前终止，返回失败 | 提示"目标目录空间不足" |
| 文件复制失败（IO异常） | `copyToPublicDirectory` 抛异常 | 记录 error_log，返回失败 | 提示"导出失败，请重试" |
| SAF 权限被撤销 | `takePersistableUriPermission` 返回 null | 引导用户重新选择目录 | 提示"目录权限已失效" |

---

#### 2.6 后台播放与通知栏模块设计

**录制状态通知（前台服务通知）**：

RecordingService 作为前台服务，必须显示一个持续通知，满足 Android 8+ 的后台运行要求，同时向用户反馈当前录制状态：

```kotlin
class RecordingNotificationManager(private val service: Service) {
    // 构建录制状态通知
    fun buildRecordingNotification(
        state: RecordingSessionState,
        metadata: TrackMetadata?
    ): Notification {
        return NotificationCompat.Builder(service, CHANNEL_ID_RECORDING)
            .setSmallIcon(R.drawable.ic_recording)
            .setContentTitle(when (state) {
                is RecordingSessionState.Recording -> "正在录制：${metadata?.title ?: "未知曲目"}"
                is RecordingSessionState.PreRecording -> "准备录制..."
                is RecordingSessionState.Finalizing -> "正在保存..."
                else -> "录制服务运行中"
            })
            .setContentText(metadata?.artist ?: "")
            .setOngoing(true)  // 不可滑动移除
            .setSilent(true)   // 不发出提示音，避免干扰录音
            .setContentIntent(openAppPendingIntent)
            .build()
    }
}
```

**通知渠道配置**：

| 渠道 ID | 渠道名称 | 重要性 | 说明 |
|:---|:---|:---|:---|
| `recording` | 录制状态 | `IMPORTANCE_LOW` | 前台服务必须通知，低重要性避免弹窗干扰 |
| `playback` | 播放控制 | `IMPORTANCE_DEFAULT` | 本地播放器后台播放时的媒体通知 |
| `export` | 导出完成 | `IMPORTANCE_DEFAULT` | 导出成功/失败的通知 |

**本地播放器后台播放设计**：

```kotlin
class LocalPlayerService : Service() {
    // 创建 MediaSessionCompat，向系统注册媒体会话
    // 支持锁屏界面显示播放信息、系统媒体控制（耳机线控、蓝牙控制）
    val mediaSession: MediaSessionCompat

    // 播放控制接口
    fun play(track: TrackRecord)
    fun pause()
    fun resume()
    fun seekTo(positionMs: Long)
    fun skipToNext()
    fun skipToPrevious()
}
```

**耳机拔出检测**：

```kotlin
// 注册 AudioManager.ACTION_AUDIO_BECOMING_NOISY 广播
// 当有线耳机拔出或蓝牙断开时，系统发送此广播
// 收到后自动暂停播放，避免声音外放
```

**关键设计决策**：
- **录制通知与播放通知分离**：录制期间显示录制通知，播放期间显示播放通知，两者不重叠。
- **MediaSession 复用**：本地播放器使用 `MediaSessionCompat` 向系统注册媒体会话，支持通知栏播放控制、锁屏显示、耳机线控。
- **后台播放保活**：本地播放器后台播放时使用 `START_STICKY` + 前台服务（`foregroundServiceType="mediaPlayback"`），与录制服务共用同一服务进程，避免多进程复杂度。
- **音频焦点抢占**：本地播放器播放时需请求音频焦点（`AudioManager.requestAudioFocus`），避免与目标录制 App 冲突。
- **隐私模式开关**：用户在设置中可开启"隐私模式"，开启后录制通知的 `setContentTitle` 仅显示"正在录制"而不显示歌曲名和艺人名。开关状态存储在 `SharedPreferences` 的 `privacy_mode_enabled` 键中，切换实时生效，无需重启服务。

**录制冲突切换通知**：

当 `SessionArbiter` 切换录制目标时，RecordingNotificationManager 立即更新通知内容：

```kotlin
fun buildSwitchNotification(fromApp: String, toApp: String): Notification {
    return NotificationCompat.Builder(service, CHANNEL_ID_RECORDING)
        .setSmallIcon(R.drawable.ic_switching)
        .setContentTitle("录制目标已切换")
        .setContentText("从「$fromApp」切换到「$toApp」")
        .setOngoing(true)
        .setSilent(true)
        .setTimeoutAfter(3000)  // 3 秒后自动消失，恢复为正常录制通知
        .addAction(R.drawable.ic_lock, "锁定 $toApp", lockSessionPendingIntent)
        .build()
}
```

---

#### 2.7 存储空间监控模块设计

录制前必须预估剩余存储空间是否满足需求，并在录制过程中持续监控。

```kotlin
interface StorageMonitor {
    // 预估本次录制所需空间（字节），按 bitrate * durationMs * safetyFactor 计算
    // safetyFactor 默认 1.2，覆盖编码开销和 ID3 标签
    fun estimateRequiredBytes(expectedDurationMs: Long, bitrateKbps: Int = 192): Long

    // 检查是否有足够空间进行录制
    // thresholdBytes = estimateRequiredBytes(expectedDurationMs, bitrate)
    suspend fun canStartRecording(thresholdBytes: Long): Result<Unit>

    // 获取当前剩余存储空间（字节），使用 StatFs 读取
    fun getAvailableBytes(): Long

    // 存储空间预警级别
    // NORMAL: >= 500MB — 正常
    // WARNING: 100MB ~ 500MB — 预警，通知 + 页面横幅，允许录制
    // CRITICAL: < 100MB — 阻止新录制，仅允许播放和导出
    fun getStorageLevel(): StorageLevel

    // 监听存储空间变化（每 30s 被动检查一次，或每次录制前主动检查）
    val storageLevel: StateFlow<StorageLevel>
}

enum class StorageLevel { NORMAL, WARNING, CRITICAL }
```

**存储空间概览信息**（设置页面展示）：

```kotlin
data class StorageOverview(
    val totalUsedBytes: Long,        // 已用空间（MP3 + 封面 + 临时文件 + DB）
    val availableBytes: Long,        // 剩余可用空间
    val trackCount: Int,             // 曲目总数
    val coverCacheSizeBytes: Long,   // 封面缓存大小
    val tempFileSizeBytes: Long,     // 临时文件大小（如有残留）
    val estimatedRemainingTracks: Int // 预估可继续录制的歌曲数（按平均大小计算）
)
```

**存储空间检查时机**：

| 时机 | 操作 | 不足时的行为 |
|:---|:---|:---|
| 录制启动前（`startCapture()` 调用时） | 按 worst-case 预估（10 分钟 × 192kbps × 1.2 ≈ 17.3MB） | 阻止录制，通知用户清理 |
| 录制过程中（`RECORDING` 状态） | 每 60s 被动检查一次 `getAvailableBytes()` | 若降至 < 50MB → emergency flush 保存半成品 → 通知用户 |
| 导出前（`exportTrack()` 调用时） | 通过 SAF 检查目标目录空间 | 提前终止并提示用户 |

---

#### 2.8 搜索与筛选模块设计

曲库搜索纯本地实现，通过 Room Query 完成，不依赖外部搜索引擎。

```kotlin
interface SearchService {
    // 关键字搜索（匹配 title / artist / album，不区分大小写的 LIKE %keyword%）
    // 返回 Flow 以支持实时更新
    fun search(query: String): Flow<List<TrackRecord>>

    // 组合筛选 + 排序
    fun searchAdvanced(
        query: String? = null,
        sourcePackage: String? = null,
        qualityFlag: String? = null,
        recordedAfter: Long? = null,
        recordedBefore: Long? = null,
        sortBy: SortField = SortField.RECORDED_AT,
        sortOrder: SortOrder = SortOrder.DESC
    ): Flow<List<TrackRecord>>
}

enum class SortField { RECORDED_AT, TITLE, ARTIST, DURATION }
enum class SortOrder { ASC, DESC }
```

**Room DAO 扩展**（TrackDao 新增查询）：

```kotlin
// 关键字搜索
@Query("""
    SELECT * FROM tracks 
    WHERE (title LIKE '%' || :query || '%' 
        OR artist LIKE '%' || :query || '%' 
        OR album LIKE '%' || :query || '%')
    ORDER BY recordedAt DESC
""")
fun search(query: String): Flow<List<TrackRecord>>

// 高级筛选（参数为 null 时忽略对应条件）
@Query("""
    SELECT * FROM tracks WHERE
    (:query IS NULL OR title LIKE '%' || :query || '%' OR artist LIKE '%' || :query || '%') AND
    (:sourcePackage IS NULL OR sourcePackage = :sourcePackage) AND
    (:qualityFlag IS NULL OR qualityFlag = :qualityFlag) AND
    (:recordedAfter IS NULL OR recordedAt >= :recordedAfter) AND
    (:recordedBefore IS NULL OR recordedAt <= :recordedBefore)
    ORDER BY CASE WHEN :sortBy = 'title' THEN title END ASC,
             CASE WHEN :sortBy = 'artist' THEN artist END ASC,
             recordedAt DESC
""")
fun advancedSearch(...): Flow<List<TrackRecord>>
```

**防抖策略**：UI 层输入关键字后使用 `debounce(300ms)`，避免每次按键触发数据库查询，减少 Room 查询负载。

---

#### 2.9 批量管理模块设计

在曲目列表页支持多选模式，批量执行导出或删除操作。

```kotlin
interface BatchOperationsManager {
    // 批量导出：对每个 trackId 依次调用 ExportManager.copyToPublicDirectory()
    // 所有文件复制到用户选择的同一目标目录（通过 SAF 获取 treeUri，一次性选择）
    // 返回成功/失败计数和失败列表
    suspend fun batchExport(
        trackIds: List<Long>,
        targetTreeUri: Uri
    ): BatchResult

    // 批量删除：调用 Repository.deleteTrack() 逐个执行（非事务，因涉及文件 I/O）
    // 保证部分失败不影响其他删除
    suspend fun batchDelete(trackIds: List<Long>): BatchResult

    // 获取批量删除前的预览信息（确认对话框展示用）
    suspend fun getDeletePreview(trackIds: List<Long>): DeletePreview
}

data class BatchResult(
    val successCount: Int,
    val failureCount: Int,
    val failures: List<BatchFailure>  // 失败的曲目列表及原因
)

data class BatchFailure(val trackId: Long, val title: String?, val reason: String)
data class DeletePreview(val totalCount: Int, val totalSizeBytes: Long)
```

**并发控制**：
- 批量导出：串行执行（每次一个文件复制，避免 IO 竞争），每个文件操作之间有 100ms 间隔。
- 批量删除：串行执行（每次一个级联删除，包含文件 IO）。
- 进度反馈：通过 `SharedFlow<BatchProgress>` 向 UI 报告当前进度（已完成数/总数）。

---

#### 2.10 播放列表模块设计

播放列表用于曲目分组管理，与 tracks 表为多对多关系。

**数据模型**：

```kotlin
@Entity(tableName = "playlists")
data class Playlist(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    @ColumnInfo(name = "name") val name: String,
    @ColumnInfo(name = "createdAt") val createdAt: Long,
    @ColumnInfo(name = "updatedAt") val updatedAt: Long
)

// 多对多关联表：播放列表 ↔ 曲目
@Entity(
    tableName = "playlist_tracks",
    foreignKeys = [
        ForeignKey(entity = Playlist::class, parentColumns = ["id"], childColumns = ["playlistId"], onDelete = ForeignKey.CASCADE),
        ForeignKey(entity = TrackRecord::class, parentColumns = ["id"], childColumns = ["trackId"], onDelete = ForeignKey.CASCADE)
    ],
    indices = [Index("playlistId"), Index("trackId")]
)
data class PlaylistTrack(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    @ColumnInfo(name = "playlistId") val playlistId: Long,
    @ColumnInfo(name = "trackId") val trackId: Long,
    @ColumnInfo(name = "position") val position: Int  // 在播放列表中的顺序位置
)
```

**DAO 接口**：

```kotlin
@Dao
interface PlaylistDao {
    @Query("SELECT * FROM playlists ORDER BY updatedAt DESC")
    fun observeAll(): Flow<List<Playlist>>

    @Insert
    suspend fun createPlaylist(playlist: Playlist): Long

    @Update
    suspend fun updatePlaylist(playlist: Playlist)

    @Query("DELETE FROM playlists WHERE id = :playlistId")
    suspend fun deletePlaylist(playlistId: Long)

    // 播放列表内的曲目（按 position 排序）
    @Query("""
        SELECT t.* FROM tracks t
        INNER JOIN playlist_tracks pt ON t.id = pt.trackId
        WHERE pt.playlistId = :playlistId
        ORDER BY pt.position ASC
    """)
    fun observeTracksInPlaylist(playlistId: Long): Flow<List<TrackRecord>>

    // 添加曲目到播放列表
    @Insert(onConflict = OnConflictStrategy.IGNORE)
    suspend fun addTrack(playlistTrack: PlaylistTrack)

    // 从播放列表移除曲目
    @Query("DELETE FROM playlist_tracks WHERE playlistId = :playlistId AND trackId = :trackId")
    suspend fun removeTrack(playlistId: Long, trackId: Long)

    // 调整曲目顺序
    @Query("UPDATE playlist_tracks SET position = :position WHERE playlistId = :playlistId AND trackId = :trackId")
    suspend fun updatePosition(playlistId: Long, trackId: Long, position: Int)
}
```

**约束**：
- 删除播放列表时 `playlist_tracks` 中对应行通过 `onDelete = CASCADE` 自动删除。
- 删除曲目时 `playlist_tracks` 中对应行通过 `onDelete = CASCADE` 自动删除，播放列表本身不受影响。
- 播放列表名称允许重复（用户可能创建同名列表）。

---

#### 2.11 重复检测模块设计

在 `PRE_RECORDING` 阶段和录制完成后两个时机检测重复，但不中断录制流程。

```kotlin
interface DuplicateDetector {
    // PRE_RECORDING 阶段调用：快速检查 identityHash 是否已存在于 tracks 表
    // 不阻塞录制启动，仅记录检测结果
    suspend fun checkDuplicate(identityHash: String): DuplicateCheckResult

    // 录制完成后，若 checkDuplicate 返回了 FOUND，发出提醒通知
    suspend fun notifyIfDuplicate(trackId: Long, identityHash: String)
}

sealed class DuplicateCheckResult {
    object NotFound : DuplicateCheckResult()
    data class Found(val existingTrackId: Long, val existingTitle: String?) : DuplicateCheckResult()
}
```

**DAO 查询**（TrackDao 新增）：

```kotlin
// 检查 identityHash 是否已存在（同来源 App）
@Query("SELECT id, title FROM tracks WHERE identityHash = :hash AND sourcePackage = :sourcePackage LIMIT 1")
suspend fun findDuplicate(hash: String, sourcePackage: String): DuplicateInfo?

data class DuplicateInfo(val id: Long, val title: String?)
```

**提醒通知设计**：

```kotlin
fun buildDuplicateNotification(trackTitle: String?, existingTrackId: Long): Notification {
    return NotificationCompat.Builder(service, CHANNEL_ID_INFO)
        .setSmallIcon(R.drawable.ic_duplicate)
        .setContentTitle("可能已存在重复曲目")
        .setContentText("「${trackTitle ?: "未知曲目"}」已在曲库中存在")
        .setContentIntent(viewTrackPendingIntent(existingTrackId))
        .setAutoCancel(true)
        .build()
}
```

**约束**：
- 重复检测不阻塞录制流程（不丢弃、不阻止），仅录制完成后提醒。
- `identityHash` 基于 title+artist+album+sourcePackage，metadata 微小差异（如 Remastered 标记）会导致不同 hash，因此不能自动判定为完全重复。
- 用户可点击提醒通知跳转到已有版本的详情页，手动决定是否删除新录制版本。

---

#### 2.12 诊断报告导出模块设计

```kotlin
interface DiagnosticReportExporter {
    // 收集诊断信息并打包为 ZIP 文件
    // 通过 SAF 让用户选择保存位置
    suspend fun exportReport(launcher: ActivityResultLauncher<Intent>): Result<Uri>

    // 收集过程（在 IO 线程执行）：
    // 1. 读取全部 error_logs（不截断）→ JSON 文件
    // 2. 读取最近 50 条 recording_sessions → JSON 文件
    // 3. 读取 SharedPreferences 键值对（脱敏处理）→ JSON 文件
    // 4. 收集设备信息（Build.MODEL, Build.VERSION.SDK_INT, ROM 版本等）→ JSON 文件
    // 5. 打包为 report_{timestamp}_{appVersion}.zip
    suspend fun collectAndPack(): Result<String>  // 返回临时 ZIP 路径
}

// 脱敏处理：对 SharedPreferences 中的敏感 key 进行值替换
// 脱敏键清单（设计时确定，供代码审查确认）：
//   - "release_keystore_path" → "[REDACTED]"
//   - "locked_session_package" → 保留（不包含敏感信息，仅包名）
//   - 其他自定义 key 默认不脱敏，除非包含 "token"/"secret"/"key"/"auth" 子串
// 诊断报告内容声明（写在 ZIP 内的 README.txt 中）：
//   "本报告不包含任何 MP3 音频文件、封面图片文件、用户凭据或 Token。"
```

**安全约束**：
- ZIP 打包过程不包含任何 MP3 文件、封面图片文件。
- SharedPreferences 键值对中，键名包含 `token`/`secret`/`key`/`auth`/`password` 子串的值统一替换为 `[REDACTED]`。
- 设备信息仅包含公开可获取的字段（`Build.MODEL`, `Build.VERSION.RELEASE`, `Build.VERSION.SDK_INT`, `Build.MANUFACTURER`），不包含 IMEI/序列号等唯一标识符。

---

#### 2.13 音频路由监听模块设计

监听蓝牙/有线耳机的连接和断开事件，向 RecordingOrchestrator 报告路由切换，防止误触发 FINALIZING。

```kotlin
interface AudioRoutingListener {
    // 初始化监听器，注册 AudioManager.AudioDeviceCallback (API 23+) 或 ACTION_AUDIO_BECOMING_NOISY 广播
    fun startListening()

    // 当前音频输出设备类型
    val currentDeviceType: StateFlow<AudioDeviceType>

    // 路由切换事件
    fun setCallback(callback: RoutingCallback)

    fun stopListening()
}

enum class AudioDeviceType { SPEAKER, WIRED_HEADSET, BLUETOOTH, UNKNOWN }

interface RoutingCallback {
    // 音频路由开始切换（设备添加/移除）
    fun onRoutingStarted(from: AudioDeviceType)

    // 音频路由切换完成（新设备稳定）
    fun onRoutingCompleted(to: AudioDeviceType)
}
```

**RecordingOrchestrator 中的路由切换处理**：

```kotlin
// 状态机中新增事件
sealed class StateEvent {
    // ... 已有事件 ...
    data class AudioRoutingStarted(val from: AudioDeviceType) : StateEvent()
    data class AudioRoutingCompleted(val to: AudioDeviceType) : StateEvent()
}

// RECORDING 状态下的处理逻辑：
//   onRoutingStarted → 启动路由切换计时器（2s）
//   onRoutingCompleted（< 2s 内）→ 重置计时器，继续录制
//   onRoutingCompleted（≥ 2s 且 PCM 持续静音）→ 触发 FINALIZING (reason=AUDIO_ROUTING)
//   onRoutingCompleted（≥ 2s 但 PCM 有有效音频）→ 重置计时器，继续录制
//   超时 2s 未收到 onRoutingCompleted → 若 PCM 持续静音则触发 FINALIZING

// FinalizeReason 枚举新增：
enum class FinalizeReason {
    // ... 已有值 ...
    AUDIO_ROUTING  // 音频路由切换超时
}
```

**设计要点**：
- 路由切换默认容忍 2s，覆盖蓝牙重连、耳机插拔的正常切换时间。
- 若 PCM 数据在切换期间仍包含有效音频（非静音），即使超过 2s 也不触发 FINALIZING，因为音频实际没有中断。
- 监听方式优先使用 `AudioManager.AudioDeviceCallback`（API 23+），准确获取当前输出设备；`ACTION_AUDIO_BECOMING_NOISY` 作为补充（仅通知耳机拔出，不提供设备类型信息）。

---

### 3. 关键流程设计

#### 3.1 录音-元数据同步状态机

这是系统的核心难点，必须解决"元数据先于音频到达"和"切歌间隙"的问题。

**线程安全模型**：

RecordingOrchestrator 接收来自多个线程的回调：
- `onPcmData` → AudioRecord 的读取线程
- `onPlaybackStateChanged` → MediaSession 回调线程（Binder 线程池）
- `onSilencePhaseChanged` → AudioCapture 内部线程
- `onRoutingStarted/onRoutingCompleted` → AudioManager 回调线程
- `startCapture/stopCapture` → UI 线程（通过 Bound Service）

**并发控制策略**：
- 状态机运行在单一 Dispatcher（`Dispatchers.Default` 的 single thread context）
- 所有回调通过 `Channel<StateEvent>` 投递事件到状态机
- 状态机按顺序处理事件，避免竞态条件

```kotlin
class RecordingOrchestrator {
    private val eventChannel = Channel<StateEvent>(Channel.UNLIMITED)
    private val orchestratorScope = CoroutineScope(Dispatchers.Default.limitedParallelism(1))

    init {
        orchestratorScope.launch {
            for (event in eventChannel) {
                processEvent(event)  // 单线程顺序处理
            }
        }
    }

    // 所有外部回调都通过 eventChannel 投递
    fun onPcmData(data: ByteArray, timestampNs: Long) {
        eventChannel.trySend(StateEvent.PcmData(data, timestampNs))
    }

    fun onPlaybackStateChanged(state: PlaybackStateCompat) {
        eventChannel.trySend(StateEvent.PlaybackStateChanged(state))
    }

    fun onRoutingStarted(from: AudioDeviceType) {
        eventChannel.trySend(StateEvent.AudioRoutingStarted(from))
    }

    fun onRoutingCompleted(to: AudioDeviceType) {
        eventChannel.trySend(StateEvent.AudioRoutingCompleted(to))
    }
}
```

```
[IDLE] ──收到PLAYING+新Metadata──► [PRE_RECORDING]
     (仅当目标App在白名单中且enabled)
                                      │
                              启动 AudioRecord
                              等待首个PCM缓冲到达
                              超时: 5秒
                                      │
                              ┌───────┴───────┐
                              │               │
                         收到PCM数据      超时(5s无数据)
                              │               │
                              ▼               ▼
                       [RECORDING]       [IDLE]
                          │            (记录error_log,
                          │             不生成文件)
           ┌──────────────┼──────────────────┬──────────────────┬──────────────────┐
           │              │                  │                  │                  │
    收到PAUSED       收到SKIP_NEXT     收到新Metadata     静音检测触发     音频路由切换>2s
   (方案B:Media     (方案B:Media     (且上一段录音      (方案A兜底:       (且PCM持续静音)
    Session状态      Session状态       duration>30s)      连续5s静音)
    变为PAUSED)      切歌事件)
           │              │                  │                  │                  │
           ▼              ▼                  ▼                  ▼                  ▼
   [FINALIZING]     [FINALIZING]        [FINALIZING]        [FINALIZING]        [FINALIZING]
           │              │                  │                  │
           └──────┬───────┘──────────────────┴──────────────────┘
                  │
           LAME flush + ID3写入
                  │
                  ▼
           [SAVING_TO_DB] (事务内执行)
                  │
           ┌──────┴──────┐
           │             │
        写入成功      写入失败
           │             │
           ▼             ▼
        [IDLE]       [IDLE]
                  (标记tagStatus=2,
                   保留MP3文件)

任意状态 ──AudioCapture中断──► [FINALIZING] (emergency flush)
任意状态 ──不可恢复异常──────► [ERROR] ──3s延迟或用户重置──► [IDLE] (清理临时文件)
```

**关键规则与精确语义**:

| 规则 | 精确说明 |
|:---|:---|
| **最短有效录音阈值** | **30秒**，指 `RECORDING` 状态的持续时间（从收到首个 PCM 数据开始计时）。**正常流程下**低于 30s 的录音视为预览/广告，自动丢弃，不写入 `tracks` 表，但写入 `error_logs` 记录原因。**例外**：当 AudioCapture 异常中断触发 emergency flush 时，不论时长均保存，标记 qualityFlag=ABNORMAL |
| **`duration` 的语义** | 状态机中"上一段录音 duration>30s"的 `duration` 指的是 **当前正在录制的片段的实际 PCM 时长**（由 LAME Encoder 累计），而非 MediaMetadata 中的歌曲时长 |
| **元数据预缓存** | 收到新 metadata 时立即异步将封面二进制数据拷贝到本地 `covers/{identityHash}.jpg`，避免 Content URI 过期。hash 算法：`SHA256(title\|artist\|album\|sourcePackage).substring(0,16)` |
| **双校验切歌** | 同时满足"收到新 metadata"且"identityHash 与当前不同"才触发保存；同一首歌的 metadata 重复推送（如进度更新）不触发 |
| **PRE_RECORDING 超时** | 5 秒内未收到首个 PCM 数据，回退到 IDLE，视为本次播放无效（可能是系统延迟或假播放） |
| **AudioRecord 启动时序** | `startCapture()` 调用时先创建 AudioRecord 实例，**异步**调用 `startRecording()`，然后进入 PRE_RECORDING 等待 PCM。若 AudioRecord 初始化失败（如权限被收回），直接进入 ERROR 状态 |
| **emergency flush** | 任意状态下 AudioCapture 中断（焦点抢占、目标 App 被杀），立即触发 LAME flush，保存已编码数据。**这是 30s 最短阈值的例外**——即使不足 30s 也保存，标记 qualityFlag=ABNORMAL。tagStatus 根据元数据是否完整决定：完整→0（正常写标签），缺失→3（待补全） |
| **静音检测兜底** | 连续 5 秒检测到静音（所有采样值 < SILENCE_AMPLITUDE_THRESHOLD），触发 `onSilencePhaseChanged(THRESHOLD)` 回调 → FINALIZING。防误判：如果静音期间收到新 metadata（切歌），重置静音计时器。触发时将本次录音所有 SHORT_SUSPECT 片段升级为 LONG_ABNORMAL |
| **白名单过滤** | 仅当播放音频的 App 在 `target_apps` 表中且 `enabled=true` 时才启动录音。非白名单 App 的 MediaSession 事件被忽略 |
| **歌曲结尾保护** | 录音最后 3s 内的静音不标记为异常（可能是歌曲 fade out），避免误判 |
| **ERROR 状态恢复** | 不可恢复异常进入 ERROR 后，需满足以下条件自动恢复到 IDLE：① 已执行 emergency flush 或资源清理；② 异常已写入 error_logs；③ 延迟 3s 确保资源释放完成。用户在 ERROR 状态下可点击"重置"立即恢复到 IDLE，丢弃未保存数据。ERROR 状态下忽略所有新 MediaSession 事件，防止状态混乱。 |
| **全静音 30s 丢弃** | 录音开始后 30 秒内持续检测到静音（所有采样值 < SILENCE_AMPLITUDE_THRESHOLD），视为无效录音，自动丢弃，不生成文件，不写入 `tracks` 表，但写入 `error_logs` 记录原因。注意：最短录音阈值（30s）优先于全静音检测；正常流程下低于 30s 的录音无论是否静音均按阈值规则丢弃，全静音 30s 丢弃主要针对 30s 以上的录音。 |
| **系统时间变更（单调时钟）** | 录音时长使用 `SystemClock.elapsedRealtime()`（单调时钟）计算，不受用户修改系统时间影响。文件时间戳 `recordedAt` 以录制开始时的 `System.currentTimeMillis()` 为准，若录制过程中检测到系统时间大幅回拨（> 1 分钟），以 `elapsedRealtime` 推算的相对时间修正 `recordedAt`，避免曲库排序混乱。 |
| **电话中断处理** | 电话接入时，系统音频焦点被电话应用抢占。检测到外部音频中断（MediaSession 状态未变但 PCM 数据变为静音，或收到 `AudioManager.ACTION_PHONE_STATE_CHANGED`）超过 3s → 触发 FINALIZING 保存当前歌曲 → 进入 IDLE。电话结束后，若白名单应用恢复播放，自动开始新录音。 |
| **Doze 模式处理** | Android Doze 模式可能限制 CPU/网络，但不应挂起前台服务。若 Doze 导致 AudioRecord 读取超时或服务被杀：①仍通过 `recording_sessions` 表抢救半成品 MP3；②记录 error_log(INFO) 注明可能因 Doze 中断。不主动监听 Doze 状态变更（过度优化），依赖现有的 `START_STICKY` + `recording_sessions` 恢复机制。 |
| **音频路由切换处理** | RECORDING 状态下收到 `AudioRoutingStarted` 事件 → 启动 2s 计时器。若 2s 内收到 `AudioRoutingCompleted` 且 PCM 仍有有效音频 → 继续录制，不触发任何转移。若 2s 内路由恢复且 PCM 持续静音 → 触发 FINALIZING (reason=AUDIO_ROUTING)。若 2s 超时未收到 `AudioRoutingCompleted` → 触发 FINALIZING (reason=AUDIO_ROUTING)，保存已录制数据。 |
| **录制前存储空间检查** | `startCapture()` 调用后、进入 PRE_RECORDING 前，调用 `StorageMonitor.canStartRecording()`。预估空间按 worst-case 计算（10 分钟 × 192kbps × 1.2 ≈ 17.3MB）。空间不足时：返回 `Result.failure(StorageFullException)` → 通知用户 → 状态保持 IDLE，不进入 PRE_RECORDING。 |

#### 3.2 文件命名与存储规范

```
/data/data/{packageName}/
├── files/
│   └── music/
│       ├── 20240115_143022_{hash}.mp3      # 本地时间(yyyyMMdd_HHmmss)+identityHash，避免文件名冲突
│       ├── covers/
│       │   └── {identityHash}.jpg          # 封面本地副本，基于 metadata 内容哈希
│       └── temp/
│           └── recording_{timestamp}.pcm   # 录音临时文件，完成后删除
├── databases/
│   ├── music_library.db                    # 主数据库（tracks + recording_sessions + error_logs + abnormal_silences + target_apps + playlists + playlist_tracks）
│   ├── music_library.db-wal                # WAL 日志
│   └── backup/
│       └── music_library_backup_{version}_{timestamp}.db  # 迁移前自动备份
└── logs/
    └── crash_{timestamp}.log               # 崩溃日志文件（补充结构化 error_logs 表）
```

#### 3.3 多 MediaSession 并发策略

系统在同一时刻 **只允许录制一个活跃 session**。由 `SessionArbiter` 模块负责仲裁：

```
┌──────────────────────────────────────────────────────┐
│              SessionArbiter (会话仲裁器)                │
├──────────────────────────────────────────────────────┤
│  1. 维护一个优先级队列：基于 target_apps 表的          │
│     priority 字段（数字越小优先级越高）                │
│     默认顺序：QQ音乐(10) > 网易云(20) > 酷狗(30)     │
│                                                      │
│  2. 当多个 session 同时活跃时：                        │
│     - 选择优先级最高的 session 作为 activeSession      │
│     - 其他 session 的 metadata 变更被忽略              │
│                                                      │
│  3. activeSession 进入 PAUSED/STOPPED 时：            │
│     - 触发当前录音的 FINALIZING                       │
│     - 重新评估队列，切换到下一个活跃 session           │
│                                                      │
│  4. 用户可在设置中手动锁定某个 session（强制优先）      │
│     锁定配置存储在 SharedPreferences.lockedSessionPackage │
└──────────────────────────────────────────────────────┘
```

**切歌间隙处理**：当 activeSession 短暂 PAUSED（< 3秒）又恢复 PLAYING 时，视为同一首歌的缓冲间隙，**不触发 FINALIZING**。判断条件：`pauseDuration < 3000ms && metadata.identityHash() 未变化`。计时器由 `RecordingOrchestrator` 维护：收到 PAUSED 事件时启动，收到 PLAYING 事件时检查间隔。

#### 3.4 应用启动初始化流程

`Application.onCreate()` 中的初始化顺序直接影响服务恢复和数据一致性：

| 步骤 | 操作 | 说明 | 耗时要求 |
| :--- | :--- | :--- | :--- |
| 1 | 加载 Native so | `System.loadLibrary("mp3lame")`，失败则记录 fatal error 并终止 | 同步，< 100ms |
| 2 | 初始化 Room Database | 执行 Migration，开发阶段可开启 `fallbackToDestructiveMigration()` | 同步，< 200ms |
| 3 | 恢复 recording_sessions | 查询残留 active session，有半成品 MP3 → salvage；无 → 清理临时文件 | 异步（协程） |
| 4 | 扫描 orphans/ 目录 | 尝试恢复孤儿文件到 tracks 表 | 异步（协程） |
| 5 | 补偿未完成的删除 | 扫描 `deleteStatus=1` 的记录，重试删除文件和数据库记录 | 异步（协程） |
| 6 | 清理过期 error_logs | 删除 30 天前的日志 | 异步（协程） |
| 7 | 权限状态预检 | 检查 MediaProjection、NotificationListenerService、RECORD_AUDIO 是否已授权 | 同步，仅读内存/SP |
| 8 | 初始化 ProjectionManager | 若 MediaProjection 已失效，标记为待重新授权，不自动弹窗 | 同步 |
| 9 | 启动 RecordingService | 若权限齐全，自动绑定服务进入 IDLE；若权限缺失，跳转引导页 | 异步（Intent） |

**关键原则**：
- 步骤 1~2 必须在主线程同步完成，总耗时控制在 **500ms 以内**，避免 Application 初始化阻塞导致 ANR。
- 步骤 3~6 使用 `CoroutineScope(Dispatchers.IO)` 异步执行，不阻塞启动流程。
- 若步骤 1（加载 so）失败，应用无法继续运行，直接弹窗提示"初始化失败"并退出。
- 步骤 7~8 的结果决定启动后进入"录制就绪"还是"权限引导页"。

---

### 4. 数据模型设计

#### 4.0 Entity 定义

以下是所有 Room Entity 的 data class 定义，与表结构一一对应。

```kotlin
@Entity(tableName = "tracks")
data class TrackRecord(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    @ColumnInfo(name = "filePath") val filePath: String,
    @ColumnInfo(name = "title") val title: String? = null,
    @ColumnInfo(name = "artist") val artist: String? = null,
    @ColumnInfo(name = "album") val album: String? = null,
    @ColumnInfo(name = "durationMs") val durationMs: Long,
    @ColumnInfo(name = "coverPath") val coverPath: String? = null,
    @ColumnInfo(name = "sourcePackage") val sourcePackage: String,
    @ColumnInfo(name = "recordedAt") val recordedAt: Long,
    @ColumnInfo(name = "tagStatus") val tagStatus: Int = 0,     // 0=未写, 1=已写入, 2=写入失败, 3=待补全
    @ColumnInfo(name = "fileSizeBytes") val fileSizeBytes: Long = 0,
    @ColumnInfo(name = "identityHash") val identityHash: String,
    @ColumnInfo(name = "qualityFlag") val qualityFlag: String = "GOOD",  // GOOD / SUSPECT / ABNORMAL
    @ColumnInfo(name = "abnormalSilenceCount") val abnormalSilenceCount: Int = 0,
    @ColumnInfo(name = "deleteStatus") val deleteStatus: Int = 0  // 0=正常, 1=删除中（用于级联删除补偿）
) {
    // 转换为 TrackMetadata（用于 ID3 标签写入）
    fun toMetadata(): TrackMetadata = TrackMetadata(
        title = title,
        artist = artist,
        album = album,
        durationMs = durationMs,
        coverUri = null,  // TrackRecord 不存储 coverUri
        coverPath = coverPath,  // 写入 ID3 时从该路径读取封面二进制数据
        sourcePackage = sourcePackage,
        isAdvertisement = false
    )
}

@Entity(tableName = "recording_sessions")
data class RecordingSessionRecord(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    @ColumnInfo(name = "targetPackage") val targetPackage: String,
    @ColumnInfo(name = "sessionToken") val sessionToken: String? = null,  // 格式：packageName_时间戳_随机数（由 serializeToken 生成）
    @ColumnInfo(name = "startedAt") val startedAt: Long,
    @ColumnInfo(name = "lastMetadataJson") val lastMetadataJson: String? = null,
    @ColumnInfo(name = "tempPcmPath") val tempPcmPath: String? = null,
    @ColumnInfo(name = "tempMp3Path") val tempMp3Path: String? = null,
    @ColumnInfo(name = "status") val status: String,  // PRE_RECORDING / RECORDING / FINALIZING
    @ColumnInfo(name = "lastUpdatedAt") val lastUpdatedAt: Long
) {
    companion object {
        // 将 MediaSessionCompat.Token 转换为可持久化的 String
        // 注意：不使用 hashCode()，因其可能随进程重启变化；使用 packageName + 时间戳 + 随机数
        fun serializeToken(token: MediaSessionCompat.Token, packageName: String): String {
            return "${packageName}_${System.currentTimeMillis()}_${(0..9999).random()}"
        }
    }
}

// 错误严重级别枚举，配合 Room TypeConverter 持久化
enum class Severity { INFO, WARNING, ERROR, FATAL }

class SeverityConverter {
    @TypeConverter
    fun fromSeverity(severity: Severity): String = severity.name

    @TypeConverter
    fun toSeverity(value: String): Severity = Severity.valueOf(value)
}

@Entity(tableName = "error_logs")
data class ErrorLogRecord(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    @ColumnInfo(name = "timestamp") val timestamp: Long,
    @ColumnInfo(name = "type") val type: String,
    @ColumnInfo(name = "severity") val severity: Severity,  // INFO / WARNING / ERROR / FATAL
    @ColumnInfo(name = "message") val message: String,
    @ColumnInfo(name = "stackTrace") val stackTrace: String? = null,
    @ColumnInfo(name = "contextJson") val contextJson: String? = null,
    @ColumnInfo(name = "relatedSessionId") val relatedSessionId: Long? = null
)

@Entity(tableName = "abnormal_silences")
data class AbnormalSilenceRecord(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    @ColumnInfo(name = "trackId") val trackId: Long,
    @ColumnInfo(name = "startTimeMs") val startTimeMs: Long,
    @ColumnInfo(name = "endTimeMs") val endTimeMs: Long,
    @ColumnInfo(name = "durationMs") val durationMs: Long,
    @ColumnInfo(name = "type") val type: String,  // SHORT_SUSPECT / LONG_ABNORMAL / INTERFERENCE
    @ColumnInfo(name = "peakAmplitude") val peakAmplitude: Int,
    @ColumnInfo(name = "createdAt") val createdAt: Long
)

@Entity(tableName = "target_apps")
data class TargetApp(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    @ColumnInfo(name = "packageName") val packageName: String,
    @ColumnInfo(name = "displayName") val displayName: String,
    @ColumnInfo(name = "priority") val priority: Int = 100,
    @ColumnInfo(name = "enabled") val enabled: Boolean = true,
    @ColumnInfo(name = "addedAt") val addedAt: Long,
    @ColumnInfo(name = "lastRecordedAt") val lastRecordedAt: Long? = null
)

// 播放列表 Entity
@Entity(tableName = "playlists")
data class Playlist(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    @ColumnInfo(name = "name") val name: String,
    @ColumnInfo(name = "createdAt") val createdAt: Long,
    @ColumnInfo(name = "updatedAt") val updatedAt: Long
)

// 播放列表 ↔ 曲目 多对多关联 Entity
@Entity(
    tableName = "playlist_tracks",
    foreignKeys = [
        ForeignKey(
            entity = Playlist::class,
            parentColumns = ["id"],
            childColumns = ["playlistId"],
            onDelete = ForeignKey.CASCADE
        ),
        ForeignKey(
            entity = TrackRecord::class,
            parentColumns = ["id"],
            childColumns = ["trackId"],
            onDelete = ForeignKey.CASCADE
        )
    ],
    indices = [Index("playlistId"), Index("trackId")]
)
data class PlaylistTrack(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    @ColumnInfo(name = "playlistId") val playlistId: Long,
    @ColumnInfo(name = "trackId") val trackId: Long,
    @ColumnInfo(name = "position") val position: Int
)
```

#### 4.1 tracks 表（已完成曲目）

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | INTEGER PK | 自增主键 |
| filePath | TEXT NOT NULL UNIQUE | MP3 绝对路径 |
| title | TEXT | 歌曲名（可能为 null） |
| artist | TEXT | 艺术家 |
| album | TEXT | 专辑名 |
| durationMs | INTEGER | 实际录音时长(ms)，来自 LAME Encoder 累计 |
| coverPath | TEXT | 本地封面文件路径（可能为 null） |
| sourcePackage | TEXT | 来源 App 包名 |
| recordedAt | INTEGER | 录制完成时间戳(ms) |
| tagStatus | INTEGER | 0=未写标签, 1=已写入, 2=写入失败, 3=待补全(元数据缺失) |
| fileSizeBytes | INTEGER | MP3 文件大小(bytes) |
| identityHash | TEXT | metadata 内容哈希，用于去重判断 |
| qualityFlag | TEXT | 质量标记：GOOD / SUSPECT / ABNORMAL |
| abnormalSilenceCount | INTEGER | 异常静音片段数量（默认 0） |

**`tagStatus` 状态流转**:

```
[录音完成] ──► tagStatus=0 (未写标签)
                  │
          尝试写入 ID3 标签
                  │
          ┌───────┼───────┐
          │       │       │
       成功    失败    元数据缺失
          │       │       │
          ▼       ▼       ▼
       tagStatus=1  tagStatus=2  tagStatus=3
          │       │       │
          │    用户可手动   用户可手动
          │    重试写入    编辑补全
          │       │       │
          └───────┴───────┘
                  │
                  ▼
              tagStatus=1 (最终态)
```

**职责分工**：`Id3TagWriter.writeTags()` 完成后，由 `RecordingOrchestrator` 根据 `Result` 更新 `tagStatus`，**不是** 由 Writer 自己更新数据库（Writer 不感知 DB）。

**`qualityFlag` 质量标记规则**：

```
录音完成后，根据 abnormal_silences 表中的记录计算 qualityFlag：

┌─────────────────────────────────────────────────────────────┐
│  qualityFlag 计算逻辑                                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 统计 abnormal_silences 表中该 trackId 的记录            │
│                                                             │
│  2. 判断规则：                                               │
│     ┌──────────────────────────────────────────────────┐   │
│     │ 条件                              │ qualityFlag  │   │
│     ├──────────────────────────────────────────────────┤   │
│     │ 无异常静音记录                    │ GOOD         │   │
│     │ 有 SHORT_SUSPECT 记录             │ SUSPECT      │   │
│     │ 有 LONG_ABNORMAL 或 INTERFERENCE  │ ABNORMAL     │   │
│     └──────────────────────────────────────────────────┘   │
│                                                             │
│  3. 优先级：ABNORMAL > SUSPECT > GOOD                       │
│     （有多种类型时，取最严重的）                              │
│                                                             │
│  4. 焦点丢失升级：如果 FINALIZING 由 SILENCE_THRESHOLD 触发，│
│     所有 SHORT_SUSPECT 先升级为 LONG_ABNORMAL，再计算        │
│                                                             │
│  5. UI 展示建议：                                            │
│     • GOOD: 正常显示                                        │
│     • SUSPECT: 显示黄色警告图标，提示"可能有短暂干扰"        │
│     • ABNORMAL: 显示红色警告图标，提示"录制可能异常"         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 4.2 abnormal_silences 表（异常静音片段）

记录每首曲目中的异常静音片段，用于质量评估和用户审核。

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | INTEGER PK | 自增主键 |
| trackId | INTEGER FK | 关联的 tracks.id |
| startTimeMs | INTEGER | 静音开始时间（相对于录音开始，ms） |
| endTimeMs | INTEGER | 静音结束时间（相对于录音开始，ms） |
| durationMs | INTEGER | 静音持续时长（ms） |
| type | TEXT | SHORT_SUSPECT / LONG_ABNORMAL / INTERFERENCE |
| peakAmplitude | INTEGER | 静音期间的最大采样值（用于判断是否有干扰） |
| createdAt | INTEGER | 记录创建时间戳 |

**静音片段类型说明**：

| 类型 | 触发条件 | 含义 | 质量影响 |
| :--- | :--- | :--- | :--- |
| SHORT_SUSPECT | 500ms ≤ duration ≤ 3s 且 peakAmplitude < THRESHOLD | 短静音，可能是歌曲间奏或轻微干扰 | SUSPECT |
| LONG_ABNORMAL | duration > 3s 且 peakAmplitude < THRESHOLD | 长静音，大概率是录制异常 | ABNORMAL |
| INTERFERENCE | duration > 500ms 且 peakAmplitude ≥ THRESHOLD | 静音期间有非零采样值（系统提示音等） | ABNORMAL |

#### 4.3 recording_sessions 表（进行中的录音）

用于服务被杀后恢复或清理。

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | INTEGER PK | 自增主键 |
| targetPackage | TEXT NOT NULL | 目标 App 包名 |
| sessionToken | TEXT | MediaSession token 标识 |
| startedAt | INTEGER | 录音开始时间戳(ms) |
| lastMetadataJson | TEXT | 最后一次收到的 metadata 序列化 JSON |
| tempPcmPath | TEXT | 临时 PCM 文件路径 |
| tempMp3Path | TEXT | 临时 MP3 文件路径（LAME 开始写入后才有） |
| status | TEXT | PRE_RECORDING / RECORDING / FINALIZING |
| lastUpdatedAt | INTEGER | 最后更新时间戳 |

**服务重启恢复逻辑**:

```kotlin
// RecordingService.onCreate()
lifecycleScope.launch {
    val activeSessions = sessionDao.getActiveSessions()
    when {
        activeSessions.isEmpty() -> {
            // 正常启动，进入 IDLE
        }
        activeSessions.size == 1 -> {
            val session = activeSessions.first()
            if (session.tempMp3Path != null && File(session.tempMp3Path).exists()) {
                // 有半成品 MP3，尝试 salvage：flush + 写入 tracks(tagStatus=0)
                // tagStatus=0 表示"未写入标签"，因为服务被杀前从未尝试过写入
                salvageInterruptedSession(session)
            } else {
                // 仅有 PCM 临时文件，丢弃
                cleanupInterruptedSession(session)
            }
        }
        else -> {
            // 多个活跃 session（不应发生），全部清理，记录 error_log
            activeSessions.forEach { cleanupInterruptedSession(it) }
            errorLogDao.insert(ErrorLogRecord(
                type = "MULTIPLE_ACTIVE_SESSIONS",
                message = "Found ${activeSessions.size} active sessions on restart",
                ...
            ))
        }
    }
}
```

#### 4.4 error_logs 表（错误诊断）

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | INTEGER PK | 自增主键 |
| timestamp | INTEGER | 错误发生时间戳(ms) |
| type | TEXT | 错误类型枚举（见 5.1） |
| severity | TEXT | 枚举：INFO / WARNING / ERROR / FATAL（通过 SeverityConverter 持久化为 String） |
| message | TEXT | 人类可读的错误描述 |
| stackTrace | TEXT | 异常堆栈（可为 null） |
| contextJson | TEXT | 上下文信息 JSON（当前 session 状态、目标 App、磁盘剩余等） |
| relatedSessionId | INTEGER | 关联的 recording_session id（可为 null） |

**自动清理**：每次服务启动时执行分级清理——① 删除 30 天前的所有日志；② 若 WARNING/INFO 日志超过 500 条，删除最旧的超出部分（按 timestamp ASC 排序）。FATAL/ERROR 日志不设数量上限（仅受 30 天时间限制）。

#### 4.5 target_apps 表（目标应用白名单）

用于管理允许录制的应用列表，支持优先级排序和启用/禁用控制。

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | INTEGER PK | 自增主键 |
| packageName | TEXT NOT NULL UNIQUE | 应用包名（如 com.tencent.qqmusic） |
| displayName | TEXT | 用户可读的应用名称（如 QQ音乐） |
| priority | INTEGER DEFAULT 100 | 优先级（10的倍数，方便在中间插入自定义项） |
| enabled | INTEGER DEFAULT 1 | 是否启用（0=禁用，1=启用） |
| addedAt | INTEGER | 添加时间戳 |
| lastRecordedAt | INTEGER | 最后一次成功录制的时间戳（可为 null） |

**默认白名单**（首次安装时预置）：

| packageName | displayName | priority |
| :--- | :--- | :--- |
| com.tencent.qqmusic | QQ音乐 | 10 |
| com.netease.cloudmusic | 网易云音乐 | 20 |
| com.kugou.android | 酷狗音乐 | 30 |
| com.kuwo.player | 酷我音乐 | 40 |
| com.ximalaya.ting.android | 喜马拉雅 | 50 |

---

### 5. 错误处理设计

#### 5.1 分层异常体系

```kotlin
// 顶层应用异常
sealed class AppException(
    override val message: String,
    override val cause: Throwable? = null
) : Exception(message, cause) {
    abstract val type: String
    abstract val recoverable: Boolean
}

// ── 不可恢复异常 ────────────────────────────
class EncoderBufferOverflowException(val level: Float) : AppException(...)
    // type = "ENCODER_BUFFER_OVERFLOW", recoverable = false
    // 触发条件：writePcm() 返回水位 = 1.0（缓冲区完全满，无法继续写入）
    // 处理：emergency flush → 保存已编码数据 → qualityFlag=ABNORMAL → ERROR → IDLE
    // 注意：水位 0.7~0.9 时仅 WARNING 日志，0.9~1.0 时丢弃旧数据（可恢复），
    //        达到 1.0 时抛异常（不可恢复，因为已丢失音频数据）

class AudioCaptureException(val errorCode: Int, override val cause: Throwable?) : AppException(...)
    // type = "AUDIO_CAPTURE_ERROR", 处理: emergency flush → ERROR → IDLE

class EncoderFatalException(override val cause: Throwable) : AppException(...)
    // type = "ENCODER_FATAL", 处理: abort → 清理临时文件 → ERROR → IDLE

class StorageFullException(val requiredBytes: Long, val availableBytes: Long) : AppException(...)
    // type = "STORAGE_FULL", recoverable = false
    // 触发路径：
    //   ① 录音前 ProjectionManager/AudioCapture 通过 StatFs 预估磁盘空间不足；
    //   ② LameEncoder.writePcm() 捕获到 IOException(ENOSPC)，将原始异常包装为 StorageFullException 返回 Result.failure；
    //   ③ Orchestrator 解包 Result，识别为 StorageFullException 后执行 flush → ERROR → IDLE。
    // 处理: flush 已编码数据 → 保存半成品 → ERROR → IDLE

class PermissionRevokedException(val permission: String) : AppException(...)
    // type = "PERMISSION_REVOKED", 处理: 停止服务 → 通知用户重新授权
```

**注意：以下事件不属于异常，不继承 AppException**：

```kotlin
// Session 切换是正常业务流程，不是错误
sealed class SessionEvent {
    data class Switched(val from: String, val to: String) : SessionEvent()
    data class Lost(val packageName: String) : SessionEvent()
}
// SessionEvent 不写入 error_logs，不触发错误恢复流程
// 仅用于 Orchestrator 内部状态转换
```

**Native Crash 的说明**：

`libmp3lame` 的 Native 崩溃（如 SIGSEGV）属于**进程级错误**，无法被 Kotlin 的 try-catch 捕获，因此不纳入 `AppException` 体系。此类崩溃会导致整个应用进程终止，依赖系统 `START_STICKY` 机制自动重启服务，并在 `onCreate()` 中通过 `recording_sessions` 表抢救半成品文件。若需崩溃分析，可接入 `Thread.setDefaultUncaughtExceptionHandler` 记录 Tombstone 信息到 `/logs/crash_{timestamp}.log`。

**已移除的异常（v2.0 → v2.1）**：

| 移除的异常 | 移除原因 |
|:---|:---|
| `AudioFocusLostException` | `OnAudioFocusChangeListener` 只能监听自己 App 的焦点变化，无法检测目标 App 的焦点丢失。焦点丢失已通过 MediaSession 状态监听 + 静音检测双重机制处理 |
| `SessionSwitchException` | Session 切换是正常业务流程，不应归类为异常。改为 `SessionEvent.Switched` |

#### 5.2 每层错误边界

```
┌─ UI Layer ─────────────────────────────────────────────┐
│  职责: 展示用户友好的错误提示，引导用户操作               │
│  处理方式:                                              │
│    - 监听 RecordingService.recordingState               │
│    - Error 状态 → 展示 Snackbar/Dialog                  │
│    - 可恢复错误 → 提供"重试"按钮                        │
│    - 不可恢复错误 → 提供"查看详情"跳转 error_logs 列表   │
│  不处理: 底层异常的具体恢复逻辑                          │
├─ Service Layer (RecordingOrchestrator) ─────────────────┤
│  职责: 决策中心，区分可恢复/不可恢复，执行恢复策略         │
│  处理方式:                                              │
│    - 模块边界统一捕获异常，转换为 AppException 后决策      │
│    - 可恢复异常 → 降级执行，记录 warning 到 error_logs   │
│    - 不可恢复异常 → emergency flush → 清理 → 通知 UI     │
│    - 所有异常都写入 error_logs 表                        │
│    - 多表写入使用 Room @Transaction                      │
├─ Module Layer (Capture/Encoder/TagWriter) ──────────────┤
│  职责: 封装模块内部错误，转换为 AppException 向上抛出      │
│  处理方式:                                              │
│    - AudioCapture: AudioRecord 错误码 → AudioCaptureException │
│    - LameEncoder: JNI 返回错误 → EncoderFatalException   │
│    - Id3TagWriter: 文件IO异常 → 返回 Result.failure()，  │
│      不抛异常（标签写入失败不应中断主流程）                │
│  原则: 模块层不直接访问 DB、不直接通知 UI                 │
└─────────────────────────────────────────────────────────┘
```

#### 5.3 关键错误场景与处理策略

| # | 错误场景 | 严重度 | 检测方式 | 处理策略 | 用户感知 |
|:--|:---|:---|:---|:---|:---|
| 1 | **LAME 编码中途失败**（JNI 返回错误码、磁盘满） | FATAL | `writePcm()` 返回 `Result.failure` | **区分两种情况**：① `Result.failure`（可控错误，如磁盘满、JNI 返回非零码）→ 尝试 `flushAndClose()` 保存已编码数据；② **Native Crash**（SIGSEGV 等进程级崩溃，不可控）→ 进程直接终止，无法执行 `abort()`；依赖 `START_STICKY` 重启后通过 `recording_sessions` 表抢救半成品 MP3 → `tagStatus` 根据元数据决定(0或3) → qualityFlag=ABNORMAL → 写入 error_logs | 通知"录音已保存（部分）" |
| 2 | **ID3 写入失败**（封面文件丢失、MP3 文件损坏） | WARNING | `writeTags()` 返回 `Result.failure` | 保留无标签 MP3 → `tagStatus=2` → 用户可在"待补全"列表手动重试 | 曲目显示"标签待补全"标记 |
| 3 | **AudioPlaybackCapture 被系统中断**（焦点抢占、目标 App 被杀） | ERROR | `onCaptureInterrupted()` 回调 | emergency flush → **即使不足 30s 也保存**（30s 规则的例外）→ tagStatus 根据元数据决定(0或3) → qualityFlag=ABNORMAL | 通知"录音因中断提前结束" |
| 4 | **前台服务被杀后恢复** | ERROR | `onStartCommand()` 检测到 `recording_sessions` 有残留 | 有半成品 MP3 → salvage 保存；无 → 清理临时文件 → 写入 error_logs | 重启后通知"上次录音已恢复/清理" |
| 5 | **MediaSession 元数据为空/乱码** | WARNING | `TrackMetadata.title == null && artist == null` | 降级为纯录音模式 → 文件名用时间戳 → `tagStatus=3`（待补全） | 曲目显示"未知曲目" |
| 6 | **并发 MediaSession 冲突** | — | `SessionArbiter` 检测到多活跃 session | 按优先级选择 active session → 其他 session 事件忽略（正常业务流程，不写入 error_logs） | 无感知 |
| 7 | **录音时长在 30s 阈值附近**（29~31s） | INFO | 状态机自然判断 | 严格按 30s 阈值执行；29.9s 丢弃，30.0s 保存 → 丢弃时写入 error_logs(INFO) | 无感知 |
| 8 | **PRE_RECORDING 超时**（5s 无 PCM） | WARNING | 超时计时器 | 回退 IDLE → 清理 AudioRecord → 写入 error_logs | 无感知（状态面板回到就绪） |
| 9 | **权限被运行时收回** | FATAL | `AudioRecord.read()` 返回错误码 | 停止服务 → 通知用户重新授权 → 清理所有临时文件 | 通知"需要重新授权录音权限" |
| 10 | **Content URI 封面过期** | WARNING | `ContentResolver.openInputStream()` 返回 null | 使用预缓存的本地副本（`covers/{hash}.jpg`）；若无副本则不嵌入封面 | 曲目无封面图 |
| 11 | **静音检测触发 FINALIZING**（5s 静音） | WARNING | `onSilencePhaseChanged(THRESHOLD)` | flush 已编码数据 → 升级所有 SHORT_SUSPECT 为 LONG_ABNORMAL → qualityFlag=ABNORMAL | 通知"录音因静音提前结束" |
| 12 | **磁盘空间不足** | ERROR | 录音前 `StatFs` 预估不足或 `writePcm()` 抛 IO 异常(ENOSPC) | 若已写入部分数据 → flush 保存半成品 → qualityFlag=ABNORMAL → tagStatus 根据元数据决定；若完全无法写入 → 清理临时文件 → ERROR → IDLE | 通知"磁盘空间不足，录音已终止" |
| 13 | **编码器缓冲区满** | ERROR | `writePcm()` 水位达到 1.0 | 抛出 EncoderBufferOverflowException → emergency flush → qualityFlag=ABNORMAL | 通知"录音已保存（可能有跳跃）" |
| 14 | **电话接入导致音频中断** | ERROR | MediaSession 状态未变但 PCM 静音 > 3s，或收到电话状态广播 | 触发 FINALIZING 保存当前歌曲 → 进入 IDLE；电话结束后若白名单应用恢复播放，自动开始新录音 | 通知"录音因电话中断已保存" |
| 15 | **全静音录音（30s 内无有效音频）** | WARNING | 录音开始后 30s 内所有采样值 < SILENCE_AMPLITUDE_THRESHOLD | 自动丢弃，不生成文件，不写入 tracks 表，记录 error_logs(INFO) 说明原因 | 无感知（状态面板回到就绪） |
| 16 | **音频路由切换超时**（蓝牙/耳机断开 > 2s） | WARNING | `AudioRoutingListener` 报告切换超时且 PCM 持续静音 | 触发 FINALIZING (reason=AUDIO_ROUTING)，保存已录制数据；若不足 30s 则按短录音规则处理（不保存，记录 error_logs） | 通知"路由切换导致录制提前结束" |
| 17 | **音频路由短暂切换**（蓝牙/耳机断开 < 2s 或切换期间 PCM 仍有有效音频） | INFO | 路由在 2s 内恢复，或 PCM 有有效数据 | 忽略，继续录制；写入 error_logs(INFO) 记录切换事件供诊断 | 无感知 |
| 18 | **Doze 模式中断**（前台服务被 Doze 挂起或杀死） | ERROR | 服务重启后 `recording_sessions` 表有残留 | 通过 `START_STICKY` 重启服务 → `recording_sessions` 表抢救半成品 MP3 → 写入 error_logs(WARNING) 注明可能因 Doze 中断 | 通知"录音已恢复/清理" |
| 19 | **批量操作部分失败**（批量删除/导出中部分文件不存在或 IO 异常） | WARNING | `BatchOperationsManager` 记录每条操作的异常 | 成功的继续执行，失败的在 `BatchResult.failures` 中返回失败列表；失败的曲目记录 error_logs(ERROR) | 弹窗显示"成功 X 首，失败 Y 首"，可查看失败列表 |
| 20 | **存储空间不足——录制前检测** | ERROR | `StorageMonitor.canStartRecording()` 返回 false | 阻止录制启动，通知用户清理空间；状态保持 IDLE | 通知"存储空间不足，无法开始新录制" |
| 21 | **存储空间不足——录制中检测** | ERROR | 录制过程中 `getAvailableBytes()` < 50MB | emergency flush → 保存半成品 → qualityFlag=ABNORMAL → 通知用户 | 通知"存储空间不足，录制已终止" |

#### 5.4 数据库事务与数据一致性

**多表写入事务**（带孤儿文件处理）：

录音完成时的数据写入在同一 Room 事务中执行：

```kotlin
// Repository 层
suspend fun finalizeTrack(
    track: TrackRecord,
    silences: List<AbnormalSilenceRecord>,
    sessionId: Long
) {
    try {
        // 事务内执行数据库操作
        roomDatabase.withTransaction {
            val trackId = trackDao.insertTrack(track)           // 1. 插入 track
            silences.forEach {                                  // 2. 插入异常静音记录
                abnormalSilenceDao.insert(it.copy(trackId = trackId))
            }
            sessionDao.deleteSession(sessionId)                  // 3. 删除 recording_session
            targetAppDao.updateLastRecordedAt(                   // 4. 更新 target_app 最后录制时间
                track.sourcePackage, System.currentTimeMillis()
            )
        }
    } catch (e: Exception) {
        // 事务失败，MP3 文件已存在但 DB 无记录 → 移动到 orphans/ 目录
        val orphanDir = File(context.filesDir, "music/orphans")
        val moveSuccess = if (orphanDir.mkdirs() || orphanDir.exists()) {
            val orphanFile = File(orphanDir, File(track.filePath).name)
            File(track.filePath).renameTo(orphanFile) && orphanFile.exists()
        } else false

        if (!moveSuccess) {
            // 二次兜底：若移动失败，保留原文件并记录，等待启动时扫描
            errorLogDao.insert(ErrorLogRecord(
                type = "ORPHAN_MOVE_FAILED",
                message = "Failed to move MP3 to orphans/, keeping original file",
                stackTrace = e.stackTraceToString(),
                contextJson = """{"filePath": "${track.filePath}"}"""
            ))
        }
        
        errorLogDao.insert(ErrorLogRecord(
            type = "FINALIZE_TRACK_FAILED",
            message = "Transaction failed, moved MP3 to orphans/",
            stackTrace = e.stackTraceToString(),
            contextJson = """{"filePath": "${track.filePath}", "moveSuccess": $moveSuccess}"""
        ))
        throw e
    }
}

// 服务启动时扫描 orphans/ 目录，尝试根据文件名中的 identityHash 恢复
suspend fun recoverOrphanFiles() {
    val orphanDir = File(context.filesDir, "music/orphans")
    if (!orphanDir.exists()) return
    
    orphanDir.listFiles()?.forEach { orphanFile ->
        try {
            // 文件名格式：20240115_143022_{identityHash}.mp3
            val parts = orphanFile.nameWithoutExtension.split("_")
            if (parts.size >= 3) {
                val identityHash = parts.last()
                // 尝试从文件名提取基本信息，创建最小化的 TrackRecord
                val track = TrackRecord(
                    filePath = orphanFile.absolutePath,
                    title = "Orphan Recording",
                    durationMs = 0,  // 需要后续分析
                    sourcePackage = "unknown",
                    recordedAt = orphanFile.lastModified(),
                    identityHash = identityHash,
                    tagStatus = 3,  // 待补全
                    qualityFlag = "ABNORMAL"
                )
                trackDao.insertTrack(track)
                orphanFile.renameTo(File(context.filesDir, "music/${orphanFile.name}"))
            }
        } catch (e: Exception) {
            // 恢复失败，保留在 orphans/ 目录，记录日志
            errorLogDao.insert(ErrorLogRecord(
                type = "ORPHAN_RECOVERY_FAILED",
                message = "Failed to recover orphan file: ${orphanFile.name}",
                stackTrace = e.stackTraceToString()
            ))
        }
    }
}
```

**删除曲目时的级联清理**（带补偿机制）：

```kotlin
// Repository 层（非事务，因为涉及文件 I/O）
suspend fun deleteTrack(trackId: Long) {
    val track = trackDao.getById(trackId) ?: return
    
    // 1. 标记为删除中（补偿机制）
    trackDao.updateDeleteStatus(trackId, deleteStatus = 1)

    try {
        // 2. 删除关联的异常静音记录
        abnormalSilenceDao.deleteByTrackId(trackId)

        // 3. 删除 MP3 文件
        val mp3File = File(track.filePath)
        if (mp3File.exists()) {
            mp3File.delete()
        }

        // 4. 删除封面文件（如果没有其他 track 引用同一封面）
        if (track.coverPath != null) {
            val refCount = trackDao.countByCoverPath(track.coverPath)
            if (refCount <= 1) {
                File(track.coverPath).delete()
            }
        }

        // 5. 删除 track 记录
        trackDao.deleteTrack(trackId)
    } catch (e: Exception) {
        // 删除失败，保留 deleteStatus=1，服务重启时会扫描并补偿
        errorLogDao.insert(ErrorLogRecord(
            type = "DELETE_TRACK_FAILED",
            message = "Failed to delete track $trackId, will retry on next startup",
            stackTrace = e.stackTraceToString(),
            contextJson = """{"trackId": $trackId, "filePath": "${track.filePath}"}"""
        ))
        throw e
    }
}

// 服务启动时扫描并补偿未完成的删除
// 注意：若文件持续被占用导致删除失败，每次启动都会重试。建议后续增加重试次数上限（如 deleteRetryCount 字段），
// 超过上限后标记为"需手动清理"，避免无限写入 error_logs。
suspend fun cleanupPendingDeletions() {
    val pendingTracks = trackDao.getByDeleteStatus(1)
    pendingTracks.forEach { track ->
        try {
            // 重试删除文件
            File(track.filePath).delete()
            track.coverPath?.let { File(it).delete() }
            abnormalSilenceDao.deleteByTrackId(track.id)
            trackDao.deleteTrack(track.id)
        } catch (e: Exception) {
            // 仍然失败，记录日志但不阻塞启动
            errorLogDao.insert(ErrorLogRecord(
                type = "DELETE_COMPENSATION_FAILED",
                message = "Failed to compensate deletion of track ${track.id}",
                stackTrace = e.stackTraceToString()
            ))
        }
    }
}
```

**ID3 重试时的清理**：

重试写入标签前，先读取现有 ID3 标签并清除，避免标签叠加：

```kotlin
suspend fun retryWriteTags(trackId: Long) {
    val track = trackDao.getById(trackId) ?: return
    // 先清除已有的不完整标签
    Id3TagWriter.clearTags(track.filePath)
    // 重新写入
    val result = Id3TagWriter.writeTags(track.filePath, track.toMetadata(), ...)
    trackDao.updateTrack(track.copy(tagStatus = if (result.isSuccess) 1 else 2))
}
```

#### 5.5 错误上报与诊断

```
┌──────────────────────────────────────────────────┐
│                 错误上报三层机制                    │
├──────────────────────────────────────────────────┤
│                                                  │
│  Layer 1: 结构化 error_logs 表                    │
│    - 所有 AppException 都写入                     │
│    - 包含完整上下文 (contextJson)                  │
│    - 分级保留策略: FATAL/ERROR 全部保留，          │
│      WARNING/INFO 保留最近 500 条                  │
│    - 30 天前日志自动清理                            │
│    - UI 提供"错误日志"页面查看（支持按严重级别筛选） │
│                                                  │
│  Layer 2: 本地 crash 日志文件                     │
│    - 未捕获异常 (Thread.UncaughtExceptionHandler) │
│    - 写入 /logs/crash_{timestamp}.log            │
│    - 包含完整堆栈 + 设备信息 + 当前状态快照        │
│                                                  │
│  Layer 3: 实时状态指示器 (debug 构建)              │
│    - 录制状态面板显示 bufferLevel、编码速率等      │
│    - 异常时高亮红色 + 显示最近一条 error_log       │
│                                                  │
│  注意: 不上报任何数据到外部服务器（纯本地诊断）     │
│  日志级别: debug 构建开启 VERBOSE/DEBUG；release 构建仅保留 INFO 及以上，防止性能损耗 │
└──────────────────────────────────────────────────┘
```

#### 5.6 数据库迁移策略

**版本号管理**：

```kotlin
@Database(
    entities = [TrackRecord::class, RecordingSessionRecord::class, 
                ErrorLogRecord::class, AbnormalSilenceRecord::class, 
                TargetApp::class, Playlist::class, PlaylistTrack::class],
    version = 1,  // 当前版本
    exportSchema = true
)
abstract class MusicDatabase : RoomDatabase() {
    // ...
}
```

**迁移策略**：

| 场景 | 策略 | 说明 |
|:---|:---|:---|
| **开发阶段**（v0.x） | `fallbackToDestructiveMigration()` | 允许破坏性迁移，直接删除旧表重建 |
| **生产环境**（v1.0+） | 显式定义 `Migration` 对象 | 保留用户数据，逐步演进 schema |

**示例迁移代码**：

```kotlin
val MIGRATION_1_2 = object : Migration(1, 2) {
    override fun migrate(database: SupportSQLiteDatabase) {
        // 添加新列
        database.execSQL(
            "ALTER TABLE tracks ADD COLUMN deleteStatus INTEGER NOT NULL DEFAULT 0"
        )
        // 添加新表
        database.execSQL(
            "CREATE TABLE IF NOT EXISTS abnormal_silences (...)"
        )
    }
}

val database = Room.databaseBuilder(context, MusicDatabase::class.java)
    .addMigrations(MIGRATION_1_2)
    .build()
```

**迁移测试**：

```kotlin
@Test
fun migrationFrom1To2_containsExpectedData() {
    val helper = createMigrationTestHelper()
    // 插入旧版本数据
    helper.insertOldData()
    // 执行迁移
    helper.migrate(MIGRATION_1_2)
    // 验证新数据
    helper.verifyNewData()
}
```

**注意事项**：
- 每次 schema 变更必须同步更新 `version` 和添加 `Migration`
- 迁移失败时回滚到旧版本，避免数据损坏
- 生产环境禁用 `fallbackToDestructiveMigration()`

**迁移前自动备份**：

Room 的 `Migration.migrate()` 没有原生的两阶段提交或回滚语义——一旦 `migrate()` 执行到一半失败（如磁盘满导致 ALTER TABLE 只执行了一半），数据库可能处于中间损坏状态。因此需要在迁移前自动备份：

```kotlin
// MusicDatabase 初始化前执行
suspend fun backupBeforeMigration(context: Context, targetVersion: Int) {
    val dbPath = context.getDatabasePath("music_library.db")
    if (!dbPath.exists()) return  // 首次安装，无需备份

    val backupDir = File(dbPath.parent, "backup").also { it.mkdirs() }
    val backupFile = File(
        backupDir,
        "music_library_backup_v${targetVersion - 1}_${System.currentTimeMillis()}.db"
    )

    try {
        dbPath.inputStream().use { input ->
            backupFile.outputStream().use { output ->
                input.copyTo(output)
            }
        }
    } catch (e: Exception) {
        // 备份失败不应阻止迁移，但需记录 WARNING
        errorLogDao.insert(ErrorLogRecord(
            type = "DB_BACKUP_FAILED",
            severity = Severity.WARNING,
            message = "Failed to backup database before migration to v$targetVersion",
            stackTrace = e.stackTraceToString()
        ))
    }
}

// 迁移失败后尝试恢复备份
suspend fun restoreFromBackup(context: Context) {
    val dbPath = context.getDatabasePath("music_library.db")
    val backupDir = File(dbPath.parent, "backup")
    val latestBackup = backupDir.listFiles()
        ?.filter { it.extension == "db" }
        ?.maxByOrNull { it.lastModified() }
        ?: return

    // 删除损坏的数据库文件
    dbPath.delete()
    dbPath.parentFile?.let { File(it, "music_library.db-wal").delete() }
    dbPath.parentFile?.let { File(it, "music_library.db-shm").delete() }

    // 恢复备份
    latestBackup.inputStream().use { input ->
        dbPath.outputStream().use { output ->
            input.copyTo(output)
        }
    }

    // 备份保留最近 3 个，旧备份自动清理
    cleanupOldBackups(backupDir, keepCount = 3)
}
```

**WAL Checkpoint 策略**：

Room WAL 模式的 `-wal` 文件在频繁写入（连续录制多首歌曲）时可能持续增长。在以下时机触发被动 checkpoint：

| 触发时机 | 方式 | 说明 |
|:---|:---|:---|
| 每次录制事务完成后（`finalizeTrack()` 成功） | `db.query("PRAGMA wal_checkpoint(PASSIVE)")` | 正常路径：每次保存一首歌曲后尝试 checkpoint |
| 每 10 次录制事务后 | `PRAGMA wal_checkpoint(FULL)` | 定期全量 checkpoint，确保 WAL 不持续膨胀 |
| 服务启动时 | `PRAGMA wal_checkpoint(TRUNCATE)` | 尝试截断 WAL，恢复干净状态 |

WAL 文件大小上限设定为 10MB。若超过上限，触发强制 checkpoint（`RESTART` 模式，阻塞写入直到完成）。

---

### 6. 权限与合规设计

#### 6.1 所需权限清单

| 权限 | 用途 | 获取方式 |
| :--- | :--- | :--- |
| `RECORD_AUDIO` | 音频采集（部分国产 ROM 要求，即使使用 AudioPlaybackCapture） | 运行时动态申请 |
| `MediaProjection` | 屏幕录制授权（AudioPlaybackCapture 必需） | 系统弹窗，用户手动同意；杀后台后失效，需重新授权 |
| `FOREGROUND_SERVICE` | 后台录音保活 | Manifest声明 |
| `FOREGROUND_SERVICE_MEDIA_PLAYBACK` | Android 14+ 前台服务类型 | Manifest声明 |
| `BIND_NOTIFICATION_LISTENER_SERVICE` | 通过 NotificationListenerService 获取外部 MediaSession | 用户手动在系统设置中开启 |
| `POST_NOTIFICATIONS` | 前台服务通知 | Android 13+ 运行时申请 |

#### 6.2 授权引导UX设计

由于涉及多个系统级权限，需设计分步引导页，按依赖顺序逐个检测：

**Step 1: MediaProjection 屏幕录制授权**
- 检测 `ProjectionManager.hasActiveProjection()`
- 未授权时展示说明页："需要授权屏幕录制以捕获系统音频" + "开始授权"按钮
- 点击后调用 `MediaProjectionManager.createScreenCaptureIntent()`，弹出系统授权对话框
- 用户在系统弹窗中点击"允许"后，`onActivityResult()` 获取 `MediaProjection` 实例
- **注意**：系统杀后台或重启后，`MediaProjection` 会失效，需在 `RecordingService` 启动前重新检测并引导

**Step 2: NotificationListenerService 通知使用权**
- 检测 `NotificationListenerService` 是否已启用（通过 `NotificationManagerCompat.getEnabledListenerPackages()`）
- 未启用时展示图文教程 + "前往设置"按钮（调用 `ACTION_NOTIFICATION_LISTENER_SETTINGS`）
- 返回App时重新检测

**Step 3: RECORD_AUDIO 录音权限**
- 运行时动态申请（`Manifest.permission.RECORD_AUDIO`）
- 部分国产 ROM 即使使用 AudioPlaybackCapture 也要求此权限

**Step 4: 关闭电池优化引导**
- 跳转系统设置（`ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`）引导用户关闭电池优化，提升后台保活概率

全部通过后，自动进入录制就绪状态。

**降级模式设计（部分权限拒绝）**：

用户在引导过程中可能拒绝部分权限，系统需以对应降级状态继续运行，而非完全不可用：

| 被拒绝权限 | 降级状态 | 可用功能 | 不可用功能 | UI 提示 |
|:---|:---|:---|:---|:---|
| **MediaProjection（屏幕录制）** | 手动录制模式 | 本地曲库播放、曲库管理、质量审核 | 自动音频录制、元数据同步 | "屏幕录制权限未开启，自动录制功能不可用。您可在设置中重新授权。" |
| **NotificationListenerService** | 半自动录制模式 | 本地曲库播放、曲库管理、手动控制录制开始/停止 | 自动根据播放状态启停录制、元数据自动抓取 | "通知访问权限未开启，无法自动检测音乐播放。您可以手动开始录制，但元数据将为空。" |
| **RECORD_AUDIO** | 纯管理/播放模式 | 本地曲库播放、曲库管理、导出、质量审核 | 任何录制功能 | "录音权限未开启，无法录制音频。您仍可以播放和管理已录制的曲目。" |
| **电池优化未关闭** | 风险提示模式 | 全部功能 | 无（但后台保活能力受限） | 首次进入主页面时弹窗提示："未关闭电池优化，系统在后台可能终止录制服务。建议前往设置关闭。" |

**降级状态持久化**：
- 使用 `SharedPreferences` 记录当前降级状态标志（`degraded_mode_flags`）。
- 每次启动时检查权限状态，若之前拒绝的权限已重新授权，自动解除对应降级状态。
- UI 层根据降级状态动态调整界面：禁用不可用的按钮、显示对应提示横幅。

#### 6.3 运行环境兼容性要求

| 项目 | 要求 | 说明 |
| :--- | :--- | :--- |
| **minSdk** | 29 (Android 10) | `AudioPlaybackCapture` 最低要求；低于此版本不提供内录功能 |
| **targetSdk / compileSdk** | 34 (Android 14) | 适配前台服务类型强制声明等新限制 |
| **ABI** | `arm64-v8a` 为主，可选 `armeabi-v7a` | libmp3lame 需预编译对应架构的 `.so` |
| **厂商适配** | 华为、小米、OPPO、vivo | 需引导用户关闭电池优化、允许自启动，防止后台被杀 |
| **进程模型** | 单进程 | 当前设计为 UI + Service 同进程，简化 Binder 通信 |

**Manifest 关键配置示例**：
```xml
<service
    android:name=".RecordingService"
    android:foregroundServiceType="mediaPlayback"
    android:exported="false" />

<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
```

---

### 7. 风险与应对策略

| 风险项 | 影响 | 应对策略 |
| :--- | :--- | :--- |
| 目标App未注册MediaSession | 无法获取元数据 | 降级为纯录音模式，文件名用时间戳，`tagStatus=3`，后续支持手动编辑标签 |
| Content URI封面过期 | 封面写入失败 | 收到metadata时立即异步拷贝到本地covers目录，写入ID3时使用本地副本；副本也丢失则不嵌入封面 |
| 系统杀后台 | 录音中断 | `START_STICKY` 前台服务 + 媒体样式通知 + 关闭电池优化引导 + `recording_sessions` 表支持重启恢复 |
| LAME编码性能不足 | 录音卡顿/丢帧 | PCM 数据放入环形缓冲区，编码在独立低优先级线程执行；三级水位线预警（0.7/0.9/1.0） |
| 目标App更新改变行为 | 状态判断失效 | 预留配置接口（SharedPreferences），可调整状态机参数（最短录音阈值、超时时间、包名优先级等） |
| 录音包含App提示音/广告 | 文件污染 | 利用 `AudioPlaybackCapture` 的 usage 过滤；结合 metadata 中 `isAdvertisement` 标志跳过 |
| 磁盘空间不足 | 录音失败 | 每次录音前检查剩余空间（预估 192kbps × 歌曲时长）；不足时提前通知用户 |
| 多 App 同时播放 | 音频混杂 | `SessionArbiter` 按优先级选择唯一 active session；其他 session 的音频仍会被系统混音录入，无法完全隔离（系统限制） |
| 古典音乐长静音段落误判 | qualityFlag 误标记 | 歌曲结尾 3s 保护机制；用户可手动将曲目标记为"正常" |

---

### 8. 开发里程碑建议

| 阶段 | 交付物 | 预估工时 |
| :--- | :--- | :--- |
| M1: 基础内录 | AudioCapture + LAME集成 + 前台服务 + 可播放的裸MP3 | 3-4天 |
| M2: 元数据同步 | MediaSession监听 + 状态机 + 元数据-录音对齐逻辑 + SessionArbiter | 3-4天 |
| M3: 标签与存储 | ID3写入 + Room数据库(全部7张表) + 文件管理 + 事务保障 + 封面EXIF清理 + 存储空间监控 | 3-4天 |
| M4: 错误处理与恢复 | 分层异常体系 + 服务重启恢复 + 错误日志页面 + 权限引导 + 录制冲突通知 + 数据库迁移备份 + Doze模式处理 | 3-4天 |
| M5: 质量检测 | 静音检测 + 异常片段标记 + qualityFlag 计算 + UI 展示 + 重复录制检测与提醒 | 2-3天 |
| M6: 本地播放器 | 播放界面 + 列表管理(含搜索筛选) + 封面显示 + 手动标签编辑 + 标签回写 + 质量审核 + 批量管理 + 播放列表 | 3-4天 |
| M7: 鲁棒性打磨 | 多机型测试 + 配置化参数 + 边界场景压测 + 诊断报告导出 + 音频路由切换测试 + WAL checkpoint验证 | 2-3天 |

---

### 9. 工程化与环境配置

#### 9.1 构建系统与工具链

| 项目 | 建议版本 / 配置 | 说明 |
| :--- | :--- | :--- |
| **Android Gradle Plugin (AGP)** | 8.2.x | 兼容 compileSdk 34 与 Kotlin 1.9 |
| **Kotlin** | 1.9.x | 与 Coroutines 1.7+ 配套 |
| **KSP** | 1.9.x-1.0.x | Room 注解处理优先使用 KSP（替代 kapt，编译更快） |
| **NDK** | 25.2.x | 编译 libmp3lame；需统一团队本地 NDK 路径 |
| **CMake** | 3.22.1（Android SDK 自带） | 通过 SDK Manager 安装，避免使用系统 CMake，减少团队环境差异 |
| **JDK** | 17 | AGP 8.x 最低要求 |

**NDK 编译配置概要**：
- 源码管理：建议以 **git submodule** 引入 `libmp3lame` 官方源码，避免预编译 `.so` 的版本黑盒。
- 输出目录：`app/build/intermediates/cmake/` 下按 ABI 输出，打包时自动合并。
- 加载方式：System.loadLibrary("mp3lame")，需在 Application 或 Service 初始化时完成。

#### 9.1.1 Build Variant 与差异化配置

| 配置项 | debug | release | 说明 |
| :--- | :--- | :--- | :--- |
| `minifyEnabled` | false | true | release 开启 R8 混淆 |
| `shrinkResources` | false | true | release 移除无用资源 |
| `fallbackToDestructiveMigration` | true | false | debug 允许破坏性迁移，快速迭代 |
| 日志级别 | VERBOSE | INFO | debug 开启完整日志，release 仅保留 INFO 及以上 |
| 诊断面板 | 显示 | 隐藏 | debug 显示 bufferLevel、实时 error_log、状态机内部状态 |
| `debuggable` | true | false | release 不可调试 |
| `nativeDebuggable` | true | false | debug 保留 Native 符号表，便于 JNI 调试 |

**Manifest 占位符**：通过 `manifestPlaceholders` 注入构建类型标识，供代码中判断当前构建类型：

```groovy
android {
    buildTypes {
        debug {
            manifestPlaceholders = [appName: "音乐录制-DEBUG"]
        }
        release {
            manifestPlaceholders = [appName: "音乐录制"]
        }
    }
}
```

#### 9.2 依赖管理

**核心依赖清单**：

| 依赖 | 用途 | 版本策略 |
| :--- | :--- | :--- |
| `androidx.room:room-*` | 本地数据库 | 使用 BOM 或统一版本 2.6+ |
| `org.jetbrains.kotlinx:kotlinx-coroutines-android` | 协程与 StateFlow | 1.7+ |
| `androidx.media:media` | MediaSessionCompat | 1.7+ |
| `com.mpatric:mp3agic` 或 `org.jaudiotagger:jaudiotagger` | ID3 标签写入 | 需评估 LGPL 许可证兼容性 |
| `libmp3lame` (NDK) | MP3 编码 | 通过 CMake 编译，版本锁定在 submodule |

**依赖锁定**：
- 使用 `gradle/libs.versions.toml`（Version Catalog）集中管理版本，避免散落在各 build.gradle 中。
- NDK 编译的 `.so` 需在 CI 或文档中记录编译参数（ABI、优化级别），确保可复现。

#### 9.3 安装与发布

| 项目 | 策略 |
| :--- | :--- |
| **构建产物** | APK（个人侧载），暂不使用 App Bundle |
| **签名** | debug 使用自动生成的 debug.keystore；release 使用独立的 `release.keystore`，密码通过环境变量或 `local.properties` 注入，不提交到版本控制 |
| **分发** | GitHub Release 或本地网盘；不计划上架应用商店 |
| **自动更新** | v1.0 阶段暂不实现；后续可考虑简单的 APK 下载 + 安装意图 |

#### 9.4 升级策略

**数据库 Schema 升级**：
- 开发阶段（v0.x）：允许 `fallbackToDestructiveMigration()`，快速迭代。
- 生产阶段（v1.0+）：每次 schema 变更必须显式定义 `Migration`，并在 `MusicDatabase` 中注册。
- 升级失败时：捕获 `IllegalStateException`，提示用户"数据需重置或降级 App"，避免崩溃。

**SharedPreferences 配置兼容**：
- 旧版本配置项若在新版本中废弃，保留读取但不写入；新增配置项使用默认值。
- 建议在应用升级时执行一次 `SharedPreferences` 版本检查，按需迁移旧 key。

**Native so 升级**：
- `.so` 文件随 APK 升级替换，不支持热更新。
- 若 so ABI 发生变更（如新增 arm64），需确保 `build.gradle` 中 `ndk.abiFilters` 与设备匹配。

#### 9.5 混淆与 R8 规则

```proguard
# Room Entity
-keep class * extends androidx.room.RoomDatabase { *; }
-keep @androidx.room.Entity class * { *; }
-keep @androidx.room.Dao class * { *; }

# JNI 调用类（LameEncoder 的 Native 方法）
-keepclasseswithmembernames class * {
    native <methods>;
}

# MediaSession
-keep class android.support.v4.media.** { *; }

# Kotlin Coroutines（防止 R8 内联导致 suspend 函数异常处理丢失）
-keepnames class kotlinx.coroutines.internal.MainDispatcherFactory {}
-keepnames class kotlinx.coroutines.CoroutineExceptionHandler {}
-keepclassmembers class kotlinx.coroutines.** { *; }

# 保留异常类型和行号（便于 release 版本的 crash 分析）
-keepattributes Exceptions, InnerClasses, Signature, SourceFile, LineNumberTable
```

**注意**：release 构建必须开启 minification 并验证 JNI 方法名未被混淆，否则 `System.loadLibrary` 成功但方法调用会报 `UnsatisfiedLinkError`。

#### 9.6 CI/CD 配置

**GitHub Actions 工作流**（`.github/workflows/ci.yml`）：

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Run lint
        run: ./gradlew lintDebug
      - name: Run unit tests
        run: ./gradlew testDebugUnitTest

  instrumented-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Run instrumented tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          arch: x86_64
          script: ./gradlew connectedCheck

  ndk-build-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive  # 拉取 libmp3lame submodule
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Verify NDK build
        run: ./gradlew assembleDebug
```

**产物管理**：
- 每次 CI 成功后，上传 debug APK 到 GitHub Actions Artifacts，保留 30 天。
- Release 版本手动触发 workflow，签名后发布到 GitHub Release。

---

### 10. 测试策略

#### 10.1 测试分层

| 层级 | 范围 | 工具 | 关键用例 |
| :--- | :--- | :--- | :--- |
| **单元测试** | SessionArbiter 优先级逻辑、RecordingOrchestrator 状态流转、identityHash 计算 | JUnit 4/5 + MockK | 多 Session 优先级排序、状态机事件顺序、空 metadata 哈希冲突 |
| **集成测试** | AudioCapture → LAME 管道、Repository 事务 | Android 仪器测试 + Hilt | PCM 数据完整性、事务失败后的 orphans 移动 |
| **UI 测试** | 录制状态面板、权限引导页 | Espresso / Compose Test | 授权流程、状态切换动画 |
| **Migration 测试** | Room schema 升级 | `MigrationTestHelper` | v1→v2 数据完整性、废弃列的默认值 |

#### 10.2 测试环境与数据

- **真机覆盖**：至少覆盖 2 台主流品牌（华为/小米）+ 1 台 Pixel（原生系统），Android 版本跨度 10~14。
- **Mock 数据**：MediaSession 回调可通过 `MediaSessionCompat` 在测试进程中模拟，无需真实播放音乐。
- **性能基准**：录制 5 分钟歌曲，验证内存曲线无泄漏、CPU 占用 < 15%（视设备而定）。

#### 10.3 模拟器测试策略

**可行性**：Android Emulator（API 29+）支持 `AudioPlaybackCapture`，可以捕获模拟器内部播放的音频流。模拟器适合作为**CI 自动化测试**和**基础功能验证**的主要环境，降低真机依赖。

**模拟器测试覆盖范围**：

| 测试项 | 模拟器可用性 | 说明 |
| :--- | :--- | :--- |
| AudioPlaybackCapture 基础采集 | ✅ 可用 | 播放本地 MP3 文件即可触发录制，验证 PCM 数据流完整性 |
| LAME 编码管道 | ✅ 可用 | 验证 PCM → MP3 的编码链路、buffer 水位线逻辑 |
| RecordingOrchestrator 状态机 | ✅ 可用 | 通过 Fake MediaSession 模拟 PLAY/PAUSE/SKIP 事件，验证状态流转 |
| Room 数据库事务 | ✅ 可用 | 与真机无差异，可在模拟器上完成全部 DAO 和 Repository 测试 |
| ID3 标签写入 | ✅ 可用 | 验证 mp3agic / jaudiotagger 对模拟器文件系统的操作 |
| 服务重启恢复 | ⚠️ 部分可用 | 模拟器可验证 `START_STICKY` 重启和 `recording_sessions` 表抢救逻辑，但无法验证厂商杀后台策略 |
| 静音检测算法 | ⚠️ 部分可用 | 可注入静音 PCM 数据验证算法阈值，但与真机音频硬件增益差异无关 |
| 后台保活 / 电池优化 | ❌ 不可用 | 模拟器无厂商定制 ROM 的电池优化策略，必须在真机上验证 |
| 音频焦点抢占 | ❌ 不可用 | 模拟器无法模拟真实多 App 音频焦点竞争，需真机配合 |
| 实际录制音质 | ❌ 不可用 | 模拟器无真实音频硬件，无法评估音质、延迟和增益 |

**模拟器测试环境搭建**：

1. **创建 AVD**：推荐 `Pixel 7 API 34`（Android 14），启用宿主机音频输出（`Emulated Performance: Automatic`）。
2. **安装测试音频**：通过 `adb push sample.mp3 /sdcard/Music/` 将测试音频推入模拟器，使用系统音乐播放器或测试 APK 播放。
3. **Fake MediaSession 方案**：在测试 APK 中创建本地 `MediaSessionCompat`，通过测试代码直接发送 `setMetadata()` 和 `setPlaybackState()`，无需真实音乐 App。
4. **CI 集成**：GitHub Actions 可使用 `reactivecircus/android-emulator-runner` 启动模拟器执行仪器测试，验证核心状态机和编码链路。

**测试分层建议**：
- **模拟器**：承担 70% 的单元测试 + 集成测试 + UI 测试，作为每次代码提交的卡点。
- **真机**：承担 30% 的验收测试（后台保活、厂商适配、音质验证），作为发版前的最终检查。

> **免责声明**: 本文档所述技术方案仅供个人学习研究与合法拥有的音乐内容个人备份使用。开发者应确保遵守相关法律法规及第三方应用服务协议，不得将本工具用于任何形式的版权侵权、商业分发或未经授权的数据采集。因不当使用产生的一切法律责任由使用者自行承担。
