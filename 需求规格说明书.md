### 个人音乐内录与元数据同步系统 - 需求规格说明书

**文档版本**: v1.2（基于 v1.1 审查后补充完善）
**适用场景**: 软件需求分析阶段，作为设计、开发、测试的输入基线
**变更摘要**: 补充曲库搜索筛选、批量管理、存储空间预警、录制冲突通知、标签回写、播放列表、录制统计、诊断报告导出、重复录制提醒、通知隐私模式、音频路由切换等需求；补充 Doze 模式、数据库迁移安全、封面 EXIF 清理、WAL 大小控制等非功能需求

---

### 1. 引言

#### 1.1 目的

本文档为“个人音乐内录与元数据同步系统”的需求规格说明书，定义系统必须满足的功能需求、非功能需求、用户场景和验收标准。本文档面向产品经理、开发工程师、测试工程师，作为设计、开发、测试活动的输入基线。

#### 1.2 范围

本系统是一款**仅限个人开发和个人使用的 Android 应用**，核心目标是在用户于第三方音乐平台（如 QQ 音乐、网易云音乐、华为音乐等）播放音乐时，自动捕获系统音频流并同步抓取歌曲元数据，最终形成可离线播放的个人本地音乐库。同时，系统提供将本地曲库中的 MP3 文件导出到公共存储目录的功能，使用户能够使用其他播放器自由播放。

**系统边界**：
- **系统内部**：音频捕获、元数据同步、MP3 编码、ID3 标签写入、本地存储管理、曲库播放、录制状态监控、文件导出。
- **系统外部**：第三方音乐 App（仅作为音频源和元数据源，不逆向、不破解）、Android 系统（权限管理、屏幕录制授权、通知访问授权、存储访问框架 SAF）。

**不包含的内容**：
- 不涉及任何 DRM 解密、逆向工程、网络分发功能。
- 不提供在线音乐搜索、在线流媒体播放、社交分享功能。
- 不支持多用户、云端同步、跨设备传输。

#### 1.3 定义与缩写

| 术语 | 定义 |
| :--- | :--- |
| **内录** | 通过系统 API 捕获设备内部播放的音频流（非麦克风录制） |
| **元数据** | 歌曲的标题、艺人、专辑、封面、时长等信息 |
| **白名单** | 用户允许录制的目标应用列表，按优先级排序 |
| **MediaSession** | Android 系统提供的媒体会话机制，第三方音乐 App 通过它推送播放状态和元数据 |
| **MediaProjection** | Android 屏幕录制授权机制，内录音频的前置权限 |
| **ID3 标签** | MP3 文件中嵌入的元数据标签（如 ID3v2.4） |
| **静音检测** | 检测录制过程中出现的异常静音片段，用于质量评估 |
| **SAF** | 存储访问框架（Storage Access Framework），Android 提供的不需要传统存储权限的文件访问方式 |
| **单调时钟** | 不受系统时间变更影响的计时器，用于保证录音时长准确性 |
| **identityHash** | 基于歌曲 title+artist+album+sourcePackage 的内容哈希值，用于同一录音会话中的切歌判断和跨会话重复检测 |
| **隐私模式** | 录制通知不显示歌曲名称和艺人名的开关设置，保护用户在公共场合的隐私 |
| **Doze 模式** | Android 6.0+ 引入的深度休眠机制，在网络断开和 CPU 限制下仍可能影响前台服务 |

---

### 2. 总体描述

#### 2.1 产品视角

本系统是一个**本地音乐备份与播放工具**，解决的核心用户痛点是：用户在第三方音乐平台（如 QQ 音乐会员、网易云音乐）上拥有大量收藏的歌曲，但这些歌曲受平台限制无法直接导出。用户希望将自己已经付费/合法拥有的音乐内容，以“所听即所得”的方式备份到本地，形成不受平台限制的个人离线曲库，并可自由导出到其他设备或播放器。

产品的价值主张：
- **自动化**：无需手动操作，播放即自动录制。
- **元数据完整**：自动抓取歌曲信息，生成带封面、歌词（后续可扩展）的标准 MP3 文件。
- **隐私隔离**：所有文件存储在应用私有目录，不被系统媒体扫描器索引。
- **质量可控**：通过静音检测标记异常录制，供用户审核。
- **自由导出**：用户可将曲目导出到公共目录，使用任意播放器播放。

#### 2.2 用户角色

本系统为单用户应用，仅有一个角色：

**个人用户（音乐爱好者）**
- 在第三方音乐平台拥有大量收藏歌曲。
- 希望将这些歌曲以 MP3 格式备份到本地，并可导出使用。
- 具备一定的 Android 使用经验，能够完成权限授权引导。
- 对音质要求适中（192kbps CBR MP3 可接受）。
- 能够理解和接受“录制质量标记”（GOOD/SUSPECT/ABNORMAL），并对异常录制进行人工审核。

#### 2.3 运行环境

| 项目 | 要求 |
| :--- | :--- |
| **操作系统** | Android 10.0（API 29）至 Android 14（API 34），需适配 Android 14 前台服务类型强制要求 |
| **设备类型** | Android 智能手机/平板 |
| **架构** | ARM64（arm64-v8a）为主，可选 ARM32（armeabi-v7a） |
| **存储空间** | 至少预留 1GB 可用空间（按 192kbps 计算，每首歌约 8~12MB），导出功能需额外空间 |
| **网络** | 无需网络连接（纯本地工具） |
| **目标应用** | 需在设备上预装 QQ音乐（v11.0+）、网易云音乐（v8.0+）、酷狗音乐（v12.0+）、酷我音乐（v10.0+）、喜马拉雅（v9.0+）等（用户自选，版本号为最低测试基线） |

#### 2.4 约束与假设

**业务约束**：
1. **合法使用**：用户仅录制自己已经合法拥有的音乐内容（如已购买、已订阅）。
2. **不触碰 DRM**：系统不尝试解密任何受 DRM 保护的音频流，仅录制系统混音器输出的音频。
3. **仅限个人使用**：录制文件不得用于商业分发或网络传播。
4. **系统限制**：无法录制未获得音频焦点的应用（系统混合器不输出）。
5. **多 App 混音**：若多个白名单应用同时播放，系统混音器会混合所有音频，无法完全隔离单个应用。系统通过仲裁机制优先录制优先级最高的应用，但无法消除混音风险。

**技术假设**：
1. 目标音乐 App 正确实现了 Android MediaSession API（推送 metadata 和 playbackState）。
2. 目标音乐 App 的音频输出使用 `AudioAttributes.USAGE_MEDIA`。
3. 用户设备运行的是标准 Android 或主流定制 ROM（华为 HarmonyOS、小米 MIUI、OPPO ColorOS、vivo OriginOS）。
4. 用户愿意授予屏幕录制授权、通知访问授权，并手动关闭电池优化、锁定应用以确保后台稳定性。
5. 用户知晓并接受：在部分强杀后台的 ROM 上，即使关闭电池优化，系统仍可能终止前台服务，导致录制中断。

#### 2.5 业务流程

**主业务流程（自动录制）**：

```
[用户打开本 App] → [完成权限引导（屏幕录制授权 + 通知访问授权 + 录音授权）]
    → [进入录制就绪状态] → [用户打开 QQ音乐播放歌曲]
    → [系统检测到 QQ音乐在白名单中且持有音频焦点] → [仲裁模块确认其优先级最高且无冲突] → [自动启动录音]
    → [同步抓取歌曲元数据，并在音频流实际到达时锁定元数据] → [用户暂停/切歌] → [自动停止并保存 MP3]
    → [MP3 嵌入标签和封面] → [存入本地曲库]
```

**子业务流程（本地播放与导出）**：

```
[用户打开本 App 本地曲库] → [浏览已录制歌曲列表] → [点击播放]
    → [显示歌曲元数据和封面] → [播放 MP3] → [用户可手动编辑标签/删除曲目]
    → [用户选择“导出”] → [调用系统文件选择器（SAF）选择目标目录] → [复制 MP3 到公共目录]
```

**异常业务流程**：

```
[录制过程中 AudioCapture 被系统中断] → [紧急保存已录制片段] → [标记为 ABNORMAL]
    → [写入 error_log] → [服务恢复到 IDLE] → [通知用户“录音因中断提前结束”]

[录制过程中电话接入] → [音频焦点被抢占] → [系统检测到焦点丢失 > 3s] → [触发 FINALIZING 保存当前歌曲]
    → [进入 IDLE，等待焦点恢复] → [若焦点恢复且在 2 分钟内检测到白名单应用播放] → [自动开始新录音]
```

---

### 3. 功能需求

#### 3.1 用例列表

| 用例编号 | 用例名称 | 参与者 | 优先级 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| UC-01 | 首次安装与权限引导 | 个人用户 | P0 | 完成屏幕录制授权、通知访问授权、录音授权，并关闭电池优化、锁定应用 |
| UC-02 | 配置目标应用白名单 | 个人用户 | P0 | 添加/移除/排序/启禁用白名单应用 |
| UC-03 | 自动录制音乐 | 系统（前台服务） | P0 | 检测到白名单应用播放时自动录制音频并抓取元数据，含多应用仲裁与元数据锁定 |
| UC-04 | 查看录制状态 | 个人用户 | P0 | 实时查看当前录制状态（IDLE/PRE_RECORDING/RECORDING/FINALIZING），以及当前录制歌曲信息 |
| UC-05 | 播放本地曲库 | 个人用户 | P1 | 浏览并播放已录制的 MP3，显示元数据和封面，支持后台播放及通知栏控制 |
| UC-06 | 管理本地曲库 | 个人用户 | P1 | 删除曲目、手动编辑 ID3 标签、重新写入封面、导出 MP3 到公共目录 |
| UC-07 | 审核录制质量 | 个人用户 | P1 | 查看 qualityFlag 和异常静音片段，标记“正常”或删除 |
| UC-08 | 查看错误日志 | 个人用户 | P1 | 浏览结构化 error_logs，排查录制失败原因，日志自动清理 |
| UC-09 | 服务被杀后恢复 | 系统 | P0 | 前台服务被杀死后，重启时恢复或清理进行中的录音，扫描临时文件 |
| UC-10 | 配置系统参数 | 个人用户 | P2 | 调整最短录音阈值、静音检测开关、优先级、导出路径等 |
| UC-11 | 导出曲目到公共目录 | 个人用户 | P1 | 通过 SAF 选择目标目录，将 MP3 文件复制到公共存储，供其他播放器使用 |
| UC-12 | 搜索与筛选曲库 | 个人用户 | P1 | 按关键字搜索、按来源App/质量标记/时间范围筛选曲目 |
| UC-13 | 批量管理曲目 | 个人用户 | P1 | 批量选择曲目进行删除或导出操作 |
| UC-14 | 播放列表管理 | 个人用户 | P2 | 创建/编辑/删除自定义播放列表，将曲目归类分组 |
| UC-15 | 存储空间管理 | 个人用户 | P0 | 查看存储空间使用情况，在空间不足前收到预警 |
| UC-16 | 录制统计与概览 | 个人用户 | P2 | 查看录制数量、总时长、来源分布等统计信息 |
| UC-17 | 诊断报告导出 | 个人用户 | P2 | 一键导出诊断报告（ZIP，含 error_logs、配置信息、系统信息，不含 MP3 文件）供问题排查 |
| UC-18 | 录制冲突处理 | 系统 | P0 | 当多个白名单应用同时播放时仲裁切换，并通知用户当前录制目标 |
| UC-19 | 重复录制提醒 | 个人用户 | P2 | 当同一首歌再次被录制时提醒用户，避免不必要的重复 |

#### 3.2 用例详细描述

**UC-01: 首次安装与权限引导**

| 项目 | 内容 |
| :--- | :--- |
| **前置条件** | 用户首次安装并打开本 App |
| **基本流程** | 1. 系统检测 MediaProjection 是否已授权 → 未授权则展示引导页，调用系统弹窗。<br>2. 检测 NotificationListenerService 是否已启用 → 未启用则跳转系统设置。<br>3. 检测 RECORD_AUDIO 权限 → 未授权则动态申请。<br>4. 引导用户关闭电池优化并锁定应用（提供操作指引）。<br>5. 全部通过后进入录制就绪状态。 |
| **异常/降级流程** | 用户拒绝 MediaProjection 授权 → 系统提示“内录功能不可用”，停留在引导页。<br>用户拒绝 NotificationListenerService → 系统提示“自动录制与元数据抓取不可用”，可进入手动录制模式（手动控制开始/停止，元数据为空）。<br>用户拒绝 RECORD_AUDIO → 系统提示“录音功能不可用”，无法使用任何录制功能。 |
| **后置条件** | 必要权限已获取，服务可正常启动。若部分权限被拒绝，系统以对应降级状态运行。 |

**UC-03: 自动录制音乐**

| 项目 | 内容 |
| :--- | :--- |
| **前置条件** | 权限齐全，前台服务运行中，白名单已配置。 |
| **触发条件** | 白名单应用开始播放音频且持有音频焦点，系统推送元数据。 |
| **基本流程** | 1. 系统通过通知监听机制检测到目标应用的播放状态变为 PLAYING。<br>2. 系统检查当前存储剩余空间是否满足录制需求（按 192kbps × 预期时长 × 1.2 安全系数预估），若不足则阻止录制并通知用户。<br>3. 会话仲裁模块检查当前是否有其他白名单应用正在录制，若是则比较优先级，若新应用优先级更高且持续播放超过 2s，则切换录制目标并通知用户；否则忽略。<br>4. 仲裁通过后，音频捕获模块创建录制实例，启动系统内录。<br>5. 进入预录制状态，等待首个有效音频数据（超时 5s）；此阶段同步进行 identityHash 重复检测（与曲库已有记录比对）。<br>6. 收到有效音频后进入录制状态，同时锁定当前元数据（标题、艺人、专辑、封面），录制过程中元数据变更不再更新，直到本段录音结束。<br>7. 编码模块开始实时编码，静音检测模块同步分析。<br>8. 检测到以下任一结束条件时触发保存：<br>&nbsp;&nbsp;&nbsp;&nbsp;• `PAUSED` — MediaSession 状态变为暂停<br>&nbsp;&nbsp;&nbsp;&nbsp;• `SKIP_NEXT` — 收到切歌事件（MediaSession 切歌回调）<br>&nbsp;&nbsp;&nbsp;&nbsp;• `NEW_METADATA` — 收到与当前不同的新 metadata（identityHash 不同，且上一段录音 ≥ 30s）<br>&nbsp;&nbsp;&nbsp;&nbsp;• `SILENCE_THRESHOLD` — 连续检测到 5s 静音（焦点丢失兜底）<br>&nbsp;&nbsp;&nbsp;&nbsp;• `AUDIO_INTERRUPTED` — AudioCapture 异常中断<br>&nbsp;&nbsp;&nbsp;&nbsp;• `SESSION_SWITCH` — SessionArbiter 切换到更高优先级 session<br>&nbsp;&nbsp;&nbsp;&nbsp;• `EMERGENCY` — 不可恢复异常触发的紧急保存<br>&nbsp;&nbsp;&nbsp;&nbsp;• `AUDIO_ROUTING` — 音频路由切换（蓝牙/耳机断开）超过 2s 且 PCM 持续静音<br>9. 编码器刷新缓冲区，标签写入模块写入标签（封面自动缩放至 500x500 JPEG，清除 EXIF 元数据），保存到本地曲库。<br>10. 若 identityHash 与已有曲目重复，录制完成后发出提醒通知。 |
| **异常流程** | 预录制超时 5s 无有效音频 → 回退待机状态，记录诊断日志。<br>音频捕获被系统中断（`AUDIO_INTERRUPTED`）→ 紧急刷新缓冲区，保存半成品，标记 ABNORMAL。<br>磁盘空间不足 → 刷新已编码数据，保存半成品，终止录音，通知用户。<br>全静音录音（30s 内无有效音频）→ 自动丢弃，不生成文件，记录诊断日志。<br>电话接入导致外部音频中断超过 3s → 触发结束保存当前歌曲（`AUDIO_INTERRUPTED`），进入待机状态。<br>系统时间变更：录音时长使用单调时钟计时，文件时间戳以录制开始时的系统时间为准，避免时间回拨导致排序混乱。<br>音频路由切换超过 2s 且 PCM 静音（`AUDIO_ROUTING`）→ 触发保存，进入待机状态。 |
| **最短录音阈值** | 正常流程下低于 30s 的录音视为预览/广告，自动丢弃；因系统中断或外部音频中断导致的紧急保存除外。 |

**UC-05: 播放本地曲库**

| 项目 | 内容 |
| :--- | :--- |
| **前置条件** | 本地曲库中已有录制完成的曲目。 |
| **基本流程** | 1. 用户打开“本地曲库”页面。<br>2. 系统展示曲目列表（支持按录制时间、标题、艺人排序）。<br>3. 用户点击曲目 → 显示封面、标题、艺人、专辑、时长。<br>4. 用户点击播放 → 使用系统播放器组件播放 MP3，支持后台播放、通知栏播放控制（播放/暂停、上一首/下一首）。<br>5. 耳机拔出时自动暂停播放。 |
| **质量标记展示** | GOOD：正常显示；SUSPECT：黄色警告图标；ABNORMAL：红色警告图标 + 提示“录制可能异常”。 |
| **续播** | 支持记住上次播放位置，下次启动时可选“继续播放”或“从头开始”。 |

**UC-11: 导出曲目到公共目录**

| 项目 | 内容 |
| :--- | :--- |
| **前置条件** | 本地曲库中已有录制完成的曲目，用户希望导出。 |
| **基本流程** | 1. 用户在曲库列表或歌曲详情页点击“导出”。<br>2. 系统调用 SAF 的 `ACTION_OPEN_DOCUMENT_TREE` 让用户选择目标目录。<br>3. 用户确认后，系统将 MP3 文件复制到所选目录。<br>4. 导出的文件保留完整的 ID3 标签和封面。<br>5. 导出成功后通知用户，并可选择“导出后是否从本地曲库删除”（默认不删除）。 |
| **异常流程** | 用户取消目录选择 → 不做任何操作。<br>目标目录空间不足 → 提示用户，终止导出。<br>文件复制失败 → 记录 error_log，通知用户重试。 |
| **后置条件** | 文件成功复制到公共目录，用户可使用其他播放器播放。 |

**UC-12: 搜索与筛选曲库**

| 项目 | 内容 |
| :--- | :--- |
| **前置条件** | 本地曲库中已有录制完成的曲目。 |
| **基本流程** | 1. 用户在曲库页面顶部搜索框输入关键字。<br>2. 系统实时匹配标题、艺人、专辑字段，展示筛选结果。<br>3. 用户可通过筛选器按来源App（QQ音乐/网易云等）、质量标记（GOOD/SUSPECT/ABNORMAL）、录制时间范围进一步缩小结果。<br>4. 筛选条件可叠加使用，结果实时更新。<br>5. 用户清除筛选条件后恢复完整列表。 |
| **后置条件** | 用户找到目标曲目，可进行播放、导出、删除等后续操作。 |

**UC-15: 存储空间管理**

| 项目 | 内容 |
| :--- | :--- |
| **前置条件** | App 已安装并运行。 |
| **基本流程** | 1. 系统在录制前自动检查剩余存储空间，预估本次录制所需空间（按 192kbps × 预期时长 × 安全系数 1.2）。<br>2. 若剩余空间 < 预估所需空间，阻止本次录制并通知用户，提示可删除旧曲目释放空间。<br>3. 用户可在设置页面查看当前存储使用概览：已用空间、曲目数、封面缓存大小、临时文件大小。<br>4. 当剩余空间低于 500MB 时，系统发出持续性预警（通知 + 页面横幅），建议用户清理。<br>5. 当剩余空间低于 100MB 时，系统阻止新的录制任务，仅允许播放和导出。 |
| **后置条件** | 用户了解存储状况，主动管理存储空间。 |

**UC-18: 录制冲突处理**

| 项目 | 内容 |
| :--- | :--- |
| **前置条件** | 白名单中配置了多个应用，前台服务运行中。 |
| **触发条件** | 用户正在录制应用 A 的音频时，白名单中的应用 B 也开始播放音频，且应用 B 的优先级高于应用 A。 |
| **基本流程** | 1. SessionArbiter 检测到应用 B 优先级更高且持续播放超过 2 秒。<br>2. 系统触发当前录音的 FINALIZING（保存应用 A 的歌曲）。<br>3. 系统向用户发送通知："录制目标已从「{应用A名称}」切换至「{应用B名称}」"。<br>4. 自动开始录制应用 B 的音频。<br>5. 若应用 B 是用户当前正在操作的 App（前台可见），通知中附带"锁定录制目标"快捷操作，防止再次被切换。 |
| **异常流程** | 用户手动锁定了应用 A 的 session → 忽略应用 B，继续录制应用 A。<br>应用 A 和应用 B 优先级相同时 → 保持当前录制目标不变（先到先录）。 |
| **后置条件** | 应用 A 的歌曲已保存，应用 B 的录制已开始，用户清楚当前录制目标。 |

**UC-13: 批量管理曲目**

| 项目 | 内容 |
| :--- | :--- |
| **前置条件** | 本地曲库中已有录制完成的曲目。 |
| **基本流程** | 1. 用户长按曲目进入多选模式。<br>2. 用户勾选多个曲目（支持全选/反选）。<br>3. 用户选择操作：批量删除、批量导出（选择目标目录一次，所有选中文件依次复制）。<br>4. 批量删除前系统弹出确认对话框，列出即将删除的曲目数量和总文件大小。<br>5. 批量导出完成后，系统汇总成功/失败数量并通知用户。 |
| **异常流程** | 批量导出中途磁盘空间不足 → 已复制的文件保留，未复制的标记失败，通知用户具体失败列表。 |
| **后置条件** | 选中曲目完成批量操作，已删除的曲目从曲库移除，已导出的曲目出现在目标目录。 |

**UC-17: 诊断报告导出**

| 项目 | 内容 |
| :--- | :--- |
| **前置条件** | 用户遇到录制问题，希望向开发者反馈。 |
| **基本流程** | 1. 用户进入设置页面，点击"导出诊断报告"。<br>2. 系统收集：全部 error_logs（不截断）、最近 50 条 recording_sessions 记录、当前系统配置（SharedPreferences 键值对，脱敏处理）、设备信息（型号、Android 版本、ROM 版本、剩余存储）、App 版本号。<br>3. 系统将以上信息打包为 ZIP 文件，通过 SAF 让用户选择保存位置。<br>4. 系统在打包时在文件名中包含时间戳和 App 版本，方便开发者区分。<br>5. 打包完成后提示用户保存位置。 |
| **约束** | 诊断报告**绝对不包含**任何 MP3 文件、封面图片文件。不包含任何可能识别用户身份的 Token/密钥。SharedPreferences 中的敏感值（如 keystore 路径）需做脱敏处理。 |
| **后置条件** | 用户获得诊断 ZIP 文件，可自行发送给开发者。 |

**UC-14: 播放列表管理**

| 项目 | 内容 |
| :--- | :--- |
| **前置条件** | 本地曲库中已有录制完成的曲目。 |
| **基本流程** | 1. 用户进入"播放列表"Tab 页。<br>2. 用户可创建自定义播放列表，输入列表名称。<br>3. 用户从曲库中选取曲目添加到播放列表（支持从搜索结果批量添加）。<br>4. 播放列表支持：创建、重命名、删除（仅删除列表不删除曲目）、调整曲目顺序。<br>5. 点击播放列表开始顺序播放。 |
| **约束** | 删除播放列表不影响曲库中的曲目文件。播放列表数据仅存储在本地 Room 数据库中。 |

**UC-16: 录制统计与概览**

| 项目 | 内容 |
| :--- | :--- |
| **前置条件** | 本地曲库中已有录制完成的曲目。 |
| **基本流程** | 1. 用户进入"统计"页面。<br>2. 系统展示：累计录制曲目数、累计录制总时长、最近 7/30 天新增曲目数。<br>3. 按来源 App 展示录制数量分布（饼图或条状图）。<br>4. 按质量标记展示比例（GOOD/SUSPECT/ABNORMAL 各占百分比）。<br>5. 展示当前存储空间使用量和预估可继续录制的歌曲数（基于平均歌曲大小）。 |
| **约束** | 所有数据从本地 Room DB 聚合查询，不涉及网络。 |

**UC-19: 重复录制提醒**

| 项目 | 内容 |
| :--- | :--- |
| **前置条件** | 本地曲库中已有录制完成的曲目。 |
| **触发条件** | 系统检测到即将开始录制的新歌的 `identityHash` 与曲库中已有曲目的 `identityHash` 相同（同 App 来源、同标题、同艺人）。 |
| **基本流程** | 1. 录制开始前（PRE_RECORDING 阶段），系统检查 identityHash 是否与 tracks 表中的已有记录重复。<br>2. 若重复，系统仍然开始录制（不中断自动化），但在录制完成后通知用户："「{标题}」可能已存在重复录制。查看已有版本 →"。<br>3. 用户点击通知跳转到已有版本的详情页，可手动决定是否删除新录制版本。<br>4. 录制仍然完成并保存（不自动丢弃），避免误判导致丢失录音。 |
| **约束** | 重复检测不阻塞录制流程，仅作为录制后的提示。identityHash 可能因 metadata 微小差异而不重复（如同一首歌的 Remastered 版本），因此不能自动丢弃。 |

#### 3.3 功能需求清单

| 需求编号 | 功能模块 | 需求描述 | 优先级 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| FR-01 | 音频捕获 | 系统能够捕获白名单应用播放的媒体音频流，采样率 44.1kHz，16bit，Stereo，格式为 PCM | P0 | 录制完成的 MP3 可正常播放，音质无明显失真，频谱差 < 3dB（参考 ITU-R BS.1387） |
| FR-02 | 元数据同步 | 实时抓取白名单应用的元数据（标题、艺人、专辑、封面、时长），并在音频流实际到达时锁定，防止切歌错配 | P0 | 95% 以上的曲目能正确抓取标题和艺人（100 首测试样本，标题不为空且非乱码） |
| FR-03 | 状态联动 | 播放自动开始录制，暂停/切歌/外部音频中断（如来电、系统提示音）持续超过 3 秒自动停止并保存 | P0 | 用户无需手动干预，录制与播放状态同步 |
| FR-04 | MP3 编码 | 将 PCM 流实时编码为 192kbps CBR MP3 | P0 | 编码后的 MP3 文件大小符合预期（约 7.5MB/5分钟） |
| FR-05 | ID3 标签 | 录音完成后自动将元数据写入 ID3v2.4 标签，含封面图片（自动缩放至 500x500 JPEG） | P0 | 使用标准 MP3 播放器可正确读取标签和封面 |
| FR-06 | 本地存储 | 文件存储在应用私有目录，SQLite 维护映射关系 | P1 | 系统媒体扫描器不索引，用户文件管理器不可见 |
| FR-07 | 本地播放 | 内置播放器支持列表播放、元数据显示、封面展示、顺序播放、暂停、进度拖动；曲库列表支持按录制时间、标题、艺人排序 | P1 | 支持顺序播放、暂停、进度拖动，元数据和封面正确显示 |
| FR-07a | 后台播放与通知栏 | 支持后台播放、通知栏播放控制（播放/暂停、上一首/下一首）、耳机拔出自动暂停 | P1 | 切出 App 后播放不中断，通知栏可控制播放状态，拔出耳机自动暂停 |
| FR-08 | 后台保活 | 前台服务持久化，通知显示当前录制状态与歌曲信息，防止系统杀后台导致录音中断 | P0 | 服务被杀后 5 秒内自动重启并恢复；用户须手动关闭电池优化并锁定应用 |
| FR-09 | 错误诊断 | 关键操作记录结构化日志，失败可追溯；FATAL/ERROR 级别全部保留，WARNING/INFO 保留最近 500 条；30 天前日志自动清理 | P1 | 用户可在 App 内查看 error_logs，页面按严重级别筛选 |
| FR-10 | 质量标记 | 检测异常静音片段，标记可疑曲目供人工审核；全静音录音自动丢弃 | P1 | 任意位置静音 > 3s 标记为 ABNORMAL，500ms~3s 标记为 SUSPECT，取最高严重级别；全静音（30s 内无有效音频）自动丢弃。注：最短录音阈值（30s）优先于全静音检测，低于 30s 的录音无论是否静音均按阈值规则丢弃 |
| FR-11 | 白名单管理 | 支持添加/移除/排序/启禁用目标应用，优先级用于多应用并发仲裁 | P0 | 用户可自定义白名单，优先级实时生效；多应用并发时，优先录制高优先级且持续播放 > 2s 的应用 |
| FR-12 | 服务恢复 | 前台服务被杀后，能通过持久化的会话记录和临时文件扫描恢复或安全清理 | P0 | 重启后自动抢救半成品 MP3 或清理临时文件（`.tmp` 后缀），扫描并清理遗留的脏数据 |
| FR-13 | 权限引导 | 分步引导用户完成屏幕录制授权、通知访问授权、录音授权，并引导关闭电池优化、锁定应用 | P0 | 首次安装后 3 分钟内完成全部授权引导；部分权限拒绝时提供降级模式说明 |
| FR-14 | 曲库管理 | 支持删除曲目、手动编辑标签、重新写入封面 | P1 | 删除时级联清理文件和数据库记录 |
| FR-15 | 质量审核 | 用户可查看异常静音片段详情，标记为“正常”或删除 | P2 | 列表展示异常片段的时间点和时长 |
| FR-16 | 导出功能 | 用户可将曲目通过 SAF 导出到公共目录，保留完整 ID3 标签和封面 | P1 | 导出的 MP3 可被其他播放器正常识别和播放 |
| FR-17 | 元数据兜底 | 当系统无法获取元数据时，文件命名为”未知歌曲_YYYYMMDD_HHMMSS.mp3”，标题字段留空 | P1 | 空元数据不导致崩溃，文件可正常播放 |
| FR-18 | 曲库搜索 | 支持按标题、艺人、专辑关键字实时搜索曲目 | P1 | 输入关键字后 300ms 内展示筛选结果 |
| FR-19 | 曲库筛选 | 支持按来源App、质量标记（GOOD/SUSPECT/ABNORMAL）、录制时间范围筛选曲目 | P1 | 筛选条件可叠加，结果实时更新 |
| FR-20 | 批量删除 | 支持多选曲目后批量删除，删除前显示确认对话框（含数量和总文件大小） | P1 | 批量删除时级联清理文件和 DB 记录，部分失败不影响其他删除 |
| FR-21 | 批量导出 | 支持多选曲目后批量导出到同一目录 | P1 | 批量导出完成后汇总成功/失败数量；目标目录空间不足时提前终止并提示 |
| FR-22 | 存储空间检查 | 录制前自动预估剩余空间是否满足本次录制需求；剩余空间 < 500MB 时发出预警；< 100MB 时阻止新录制 | P0 | 空间不足时阻止录制并提示用户清理；预警在通知栏和页面内同时展示 |
| FR-23 | 录制冲突通知 | 当仲裁器切换录制目标时，通知用户当前录制的应用已变更，并显示新目标应用名称 | P0 | 切换后 1s 内发出通知 |
| FR-24 | 标签编辑后回写 | 用户手动编辑曲目标题/艺人/专辑后，系统自动重新写入 ID3v2.4 标签到 MP3 文件（含封面） | P1 | 编辑保存后标签回写成功，第三方播放器可读取更新后的标签 |
| FR-25 | 播放列表 | 支持创建/编辑/删除自定义播放列表，从曲库选取曲目添加 | P2 | 播放列表删除不影响原曲目文件 |
| FR-26 | 录制统计 | 展示累计录制曲目数、总时长、来源分布、质量比例、存储空间预估可录制数 | P2 | 统计数据来自 Room DB 聚合查询，加载时间 < 1s |
| FR-27 | 诊断报告导出 | 一键打包 error_logs（全量）、最近 50 条 session 记录、脱敏配置、设备信息、App 版本为 ZIP 文件 | P2 | ZIP 文件不含任何 MP3/封面文件；脱敏处理覆盖敏感 key |
| FR-28 | 重复录制提醒 | 检测到新录制歌曲的 identityHash 与曲库已有记录重复时，在录制完成后提醒用户 | P2 | 提醒不阻塞录制流程，用户可手动删除重复版本 |
| FR-29 | 前台服务通知隐私模式 | 提供设置开关，开启后录制通知不显示具体歌曲名称，仅显示”正在录制” | P1 | 隐私模式开启后通知标题不含歌曲名和艺人名 |
| FR-30 | 音频路由切换处理 | 录制过程中蓝牙/有线耳机断开时，系统忽略短暂音频路由切换（< 2s），不触发 FINALIZING；间断超过 2s 则保存当前曲目 | P0 | 耳机插拔不导致误触发停止录制；路由切换超时后正常保存曲目 |

---

### 4. 非功能需求

#### 4.1 性能需求

| 需求编号 | 需求描述 | 验收标准 |
| :--- | :--- | :--- |
| NFR-01 | 启动时间 | Application.onCreate() 同步初始化耗时：高端设备 < 300ms，中端设备 < 500ms，低端设备 < 800ms |
| NFR-02 | 录制延迟 | 从检测到 PLAYING 到首个有效音频数据到达 < 1s（进入录制状态）；预录制超时兜底为 5s |
| NFR-03 | 编码性能 | 5 分钟录制，CPU 占用 < 15%（参照设备：骁龙 8 Gen 1），内存占用 < 200MB |
| NFR-04 | 播放性能 | 本地曲库 1000 首歌曲，列表滑动无卡顿，首屏加载 < 1s |
| NFR-05 | 存储效率 | 192kbps CBR，5 分钟歌曲约 7.5MB；1000 首歌曲约 7.5GB；封面缩放后单张 < 50KB |
| NFR-05a | 曲库列表加载 | 1000 首曲目时首屏加载 < 1s；10000 首曲目时首屏加载 < 2s。必须采用分页或虚拟列表策略，禁止一次性加载全部列表项 |
| NFR-05b | 封面图片加载 | 列表缩略图内存占用 < 100KB/张；详情页封面 < 2MB；列表快速滑动时不阻塞主线程 |
| NFR-05c | WAL 日志大小 | Room WAL 文件不超过 10MB，每次录制完成后触发一次被动 checkpoint |

#### 4.2 安全与隐私需求

| 需求编号 | 需求描述 | 验收标准 |
| :--- | :--- | :--- |
| NFR-06 | 隐私隔离 | 所有文件存储在 `/data/data/{packageName}/`，不被系统媒体扫描器索引 | 第三方文件管理器无法访问 |
| NFR-07 | 权限最小化 | 仅申请必要权限（MediaProjection、NotificationListenerService、RECORD_AUDIO、前台服务）；导出功能使用 SAF，不申请外部存储读写权限 | 不申请 `READ_EXTERNAL_STORAGE` 或 `WRITE_EXTERNAL_STORAGE` |
| NFR-08 | 数据不上云 | 不上报任何数据到外部服务器，error_logs 仅限本地存储 | 抓包验证无网络请求 |
| NFR-09 | 合规边界 | 不涉及 DRM 解密、逆向工程、网络分发；Android 14 前台服务类型声明正确 | 安全扫描无违规代码，Android 14 上 `foregroundServiceType` 声明为 `mediaPlayback` 或 `dataSync` |
| NFR-09a | 封面 EXIF 清理 | 缓存封面图片时必须清除 EXIF 元数据（GPS、设备信息等），仅保留图像像素数据 | 缓存的封面文件不含 EXIF 信息 |
| NFR-09b | 诊断报告隐私 | 诊断报告 ZIP 文件不得包含 MP3 文件、封面图片、用户 Token/密钥；SharedPreferences 中的敏感键值需脱敏 | 代码审查确认脱敏键值清单完整 |
| NFR-09c | 通知隐私保护 | 录制通知在隐私模式下不显示歌曲标题和艺人名 | 隐私模式开关实时生效 |

#### 4.3 可用性需求

| 需求编号 | 需求描述 | 验收标准 |
| :--- | :--- | :--- |
| NFR-10 | 容错性 | 元数据缺失或录音异常时不崩溃，生成无标签 MP3 并标记为待补全；封面图片过大或格式异常时自动缩放/转换 | 空元数据、超大封面、WebP 等格式异常封面场景 App 不崩溃 |
| NFR-11 | 可恢复性 | 关键异常后服务可自动恢复，用户可手动重置；录音过程中磁盘满时，以最大努力保存已编码数据 | 异常后 3s 内自动恢复待机状态；磁盘满时保存半成品 MP3 |
| NFR-12 | 数据一致性 | 多表写入使用事务；删除曲目时级联清理文件和数据库记录；临时文件使用专用后缀，启动时扫描清理 | 事务失败时文件移动到隔离目录，不丢失；启动时清理所有临时文件 |
| NFR-13 | 后台稳定性 | 前台服务在主流厂商 ROM 下可稳定运行。用户必须手动关闭电池优化并锁定应用；在部分强杀后台的 ROM 上，无法保证 30 分钟以上稳定录制，系统将提示用户风险 | 华为/小米/OPPO 真机测试，关闭电池优化后录制 30 分钟无中断；未关闭时提示用户 |
| NFR-13a | Doze 模式兼容 | Android Doze 模式下前台服务不应被挂起；若系统进入深度 Doze 导致录制中断，重启后通过 recording_sessions 表抢救半成品 | 模拟器中通过 `adb shell dumpsys deviceidle force-idle` 测试 Doze 场景 |
| NFR-13b | 数据库迁移安全 | 数据库 schema 升级前必须自动备份当前数据库文件到 `/databases/backup/`；迁移失败时恢复备份并通知用户 | 迁移失败场景下数据不丢失，旧版本数据可恢复 |
| NFR-13c | error_logs 分级保留 | FATAL/ERROR 日志全部保留；WARNING/INFO 保留最近 500 条；30 天前日志自动清理。日志上限从 100 条调整为 500 条（WARNING+）+ 全部 FATAL/ERROR | 日志表记录数不超过设定上限 |

#### 4.4 兼容性与环境需求

| 需求编号 | 需求描述 | 验收标准 |
| :--- | :--- | :--- |
| NFR-14 | Android 版本 | 支持 Android 10（API 29）至 Android 14（API 34），适配 Android 14 前台服务强制要求 | 模拟器 API 29/34 测试通过，Android 14 无前台服务异常 |
| NFR-15 | 设备覆盖 | 覆盖主流品牌（华为、小米、OPPO、vivo）和 Pixel | 至少 2 台国产 + 1 台 Pixel 真机测试 |
| NFR-16 | 架构兼容 | 支持 arm64-v8a，可选 armeabi-v7a | APK 包含对应 ABI 的 .so 文件 |
| NFR-17 | 目标应用兼容 | 兼容 QQ音乐（v11.0+）、网易云音乐（v8.0+）、酷狗音乐（v12.0+）、酷我音乐（v10.0+）、喜马拉雅（v9.0+） | 上述 App 指定版本及以上录制测试通过 |
| NFR-17a | 默认白名单可配置 | 预置白名单定义在可独立更新的 JSON 配置文件中（存放于 assets/），App 启动时读取并比对更新，不以硬编码方式写入 DB 初始化逻辑 | 新增默认 App 或修改优先级时，仅需更新 JSON 文件，无需改代码 |
| NFR-17b | 元数据测试样本规范 | 验收测试中元数据抓取成功率测试需覆盖：5个App × 20首歌曲 = 100首，其中包含中文、英文、日文、韩文、纯音乐、特殊字符（如『』〜・）六种类型各占合理比例 | 测试报告附测试样本清单和每种类型的成功率 |

---

### 5. 验收标准

#### 5.1 功能验收标准

| 功能 | 验收条件 | 测试方法 |
| :--- | :--- | :--- |
| 自动录制 | 在 QQ音乐播放一首 4 分钟歌曲，本 App 自动录制完成，生成完整 MP3 | 真机手动测试 |
| 元数据抓取与锁定 | 录制完成后，MP3 标签包含正确的标题、艺人、专辑、封面；快速切歌时标签不串乱 | 使用第三方标签查看工具验证，连续切歌 10 次测试 |
| 状态联动 | 播放→录制开始；暂停→录制停止并保存；切歌→上一首保存，新一首开始录制；外部音频中断 3s→保存 | 真机手动测试，模拟电话中断 |
| 后台保活 | 未关闭电池优化时提示风险；关闭后播放 30 分钟歌单，录制不中断，生成的 MP3 数量与歌曲数量一致 | 真机长时测试 |
| 服务恢复 | 录制过程中强制停止 App，重启后抢救半成品或清理临时文件，无残留临时文件 | 真机/模拟器测试 |
| 权限引导 | 首次安装后，按引导完成全部授权，进入录制就绪状态；部分拒绝时进入降级模式 | UI 测试 |
| 本地播放 | 录制的 MP3 在本 App 内可正常播放，封面和元数据正确显示，支持后台播放和通知栏控制 | 真机手动测试 |
| 质量标记 | 录制过程中模拟音频焦点被抢占（播放系统提示音），生成的曲目标记为 ABNORMAL；全静音 30s 自动丢弃 | 仪器测试 |
| 导出功能 | 通过 SAF 导出 MP3 到公共目录，其他播放器可正常识别和播放 | 真机手动测试 |
| 边界处理 | 封面过大、磁盘满、电话中断、系统时间变更等场景不崩溃，半成品可恢复 | 边界场景压测 |
| 曲库搜索 | 在 1000 首曲目中输入关键字，300ms 内展示匹配结果 | UI 测试 |
| 批量删除 | 选中 50 首曲目批量删除，所有曲目文件和 DB 记录正确清理；模拟其中 5 首文件不存在，其余 45 首正常删除 | 真机测试 |
| 批量导出 | 选中 20 首曲目批量导出到目标目录，所有曲目正确复制；模拟目标目录空间不足，提前终止并提示 | 真机测试 |
| 存储空间检查 | 剩余空间 < 500MB 时触发预警；< 100MB 时阻止录制；恢复空间后自动解除限制 | 仪器测试 / 模拟器注入 |
| 录制冲突通知 | 两个白名单 App 同时播放，仲裁切换后 1s 内收到切换通知 | 真机测试 |
| 标签编辑回写 | 编辑曲目标题/艺人/专辑后，使用第三方 ID3 查看工具确认 MP3 文件标签已更新 | 真机测试 |
| 前台通知隐私模式 | 开启隐私模式后，录制通知标题不含歌曲名；关闭后恢复显示歌曲名 | UI 测试 |
| 音频路由切换 | 录制中插拔蓝牙/有线耳机，< 2s 的路由切换不触发停止；模拟 > 2s 切换触发正常保存 | 真机测试 |
| 诊断报告导出 | 导出诊断 ZIP，确认不含 MP3/封面文件，SharedPreferences 敏感键值已脱敏 | 手动测试 + 代码审查 |
| 播放列表 | 创建/编辑/删除播放列表，添加曲目并播放 | UI 测试 |
| 录制统计 | 统计页面数据与 DB 实际数据一致，加载时间 < 1s | UI 测试 |
| 重复录制提醒 | 第二次录制同一首歌（同 identityHash）后收到提醒通知，可点击查看已有版本 | 真机测试 |

#### 5.2 非功能验收标准

| 指标 | 目标值 | 测试方法 |
| :--- | :--- | :--- |
| 启动耗时 | < 500ms（参照：骁龙 8 Gen 1） | 仪器测试（记录 Application.onCreate 耗时） |
| 录制音质 | 192kbps CBR，频谱差 < 3dB，无明显杂音/断音 | 人工试听 + 频谱分析 |
| 内存占用 | 录制过程中 < 200MB | Android Profiler |
| 存储空间 | 5 分钟歌曲 ≈ 7.5MB，封面 < 50KB | 文件大小统计 |
| 崩溃率 | < 0.1%（连续录制 100 首歌无崩溃） | 压力测试 |
| 元数据抓取成功率 | > 95%（100 首测试样本，包含中/英/日/韩/纯音乐/特殊字符六种类型） | 自动化脚本 + 人工核验 |
| Doze 模式兼容 | 模拟器 Doze 模式下录制 ≥ 5 分钟不中断；若系统强杀，重启后抢救半成品 | `adb shell dumpsys deviceidle force-idle` |
| 数据库迁移安全 | 模拟迁移失败场景，确认备份文件存在且数据可恢复 | Migration 测试 |
| 封面 EXIF 清理 | 缓存封面文件不含 EXIF 数据（GPS、设备信息等） | 代码审查 + 文件分析 |
| 曲库列表分页 | 10000 首曲目滑动流畅（帧率 ≥ 55fps），内存占用不超过 200MB | Android Profiler |
| WAL 日志大小 | 连续录制 10 首歌曲后 WAL 文件 < 10MB | 文件大小统计 |

---

### 6. 需求追溯矩阵

| 需求编号 | 设计文档章节 | 测试章节 | 里程碑 |
| :--- | :--- | :--- | :--- |
| FR-01 ~ FR-04 | 2.2（音频捕获模块、编码模块）、3.1（状态机） | 10.1（集成测试：音频捕获 → 编码管道） | M1: 基础内录 |
| FR-02, FR-05, FR-17 | 2.2（元数据监听模块、标签写入模块） | 10.1（集成测试：标签写入、元数据锁定） | M2: 元数据同步 |
| FR-03, FR-12 | 3.1（状态机）、3.4（启动初始化） | 10.1（单元测试：状态机流转） | M2: 元数据同步 |
| FR-16 | 2.5（文件导出模块设计） | 10.1（导出流程测试） | M3: 标签与存储 |
| FR-06, FR-14 | 4.0（数据模型定义）、5.4（事务与补偿） | 10.1（Migration 测试） | M3: 标签与存储 |
| FR-07, FR-15 | 2.1（UI 层：本地曲库播放器） | 10.1（UI 测试） | M6: 本地播放器 |
| FR-07a | 2.6（后台播放与通知栏模块设计） | 10.1（后台播放测试、通知栏控制测试） | M6: 本地播放器 |
| FR-08, FR-13 | 6.1（权限清单）、6.2（授权引导 UX）、2.2（模块职责：通知显示） | 10.1（UI 测试：授权流程、降级模式） | M4: 错误处理与恢复 |
| FR-09, FR-10 | 2.2（静音检测）、5.1（分层异常体系） | 10.1（单元测试：静音检测算法、全静音丢弃） | M5: 质量检测 |
| FR-11 | 2.2（会话仲裁模块）、优先级管理 | 10.1（多应用并发测试） | M2: 元数据同步 |
| NFR-01 ~ NFR-05 | 3.4（启动初始化）、9.1（构建工具链） | 10.2（性能基准） | M7: 鲁棒性打磨 |
| NFR-06 ~ NFR-09 | 1.3（非功能性需求）、6.1（权限最小化） | 安全扫描 + 抓包测试 | M7: 鲁棒性打磨 |
| NFR-10 ~ NFR-13 | 5.3（关键错误场景）、边界场景处理 | 边界场景压测 | M7: 鲁棒性打磨 |
| NFR-14 ~ NFR-17 | 6.3（运行环境兼容性）、Android 14 适配 | 10.2（真机覆盖） | M7: 鲁棒性打磨 |
| FR-18 ~ FR-19 | 2.2（曲库搜索与筛选模块） | 10.1（搜索筛选测试） | M6: 本地播放器 |
| FR-20 ~ FR-21 | 2.2（批量管理模块） | 10.1（批量操作测试） | M6: 本地播放器 |
| FR-22 ~ FR-23 | 2.2（录制管理模块） | 10.1（存储空间与冲突通知测试） | M4: 错误处理与恢复 |
| FR-24 | 2.2（标签写入模块） | 10.1（标签回写测试） | M3: 标签与存储 |
| FR-25 | 2.1（播放列表 UI） | 10.1（播放列表测试） | M6: 本地播放器 |
| FR-26 | 2.1（统计页面 UI） | 10.1（统计展示测试） | M6: 本地播放器 |
| FR-27 | 2.2（诊断报告模块） | 10.1（诊断报告导出测试） | M7: 鲁棒性打磨 |
| FR-28 | 2.2（录制管理模块） | 10.1（重复提醒测试） | M5: 质量检测 |
| FR-29 | 2.2（通知模块） | 10.1（通知隐私模式测试） | M4: 错误处理与恢复 |
| FR-30 | 2.2（音频捕获模块）、3.1（状态机） | 10.1（路由切换测试） | M4: 错误处理与恢复 |
| NFR-05a ~ NFR-05c | 9.1（构建工具链） | 10.2（性能基准） | M7: 鲁棒性打磨 |
| NFR-09a ~ NFR-09c | 6.1（权限最小化）、2.2（通知模块） | 安全扫描 + 代码审查 | M7: 鲁棒性打磨 |
| NFR-13a ~ NFR-13c | 5.x（错误恢复设计） | 10.2（压力测试） | M7: 鲁棒性打磨 |
| NFR-17a ~ NFR-17b | 9.1（构建系统与工具链）、2.3（白名单机制） | 10.1（兼容性测试） | M7: 鲁棒性打磨 |

---

> **免责声明**: 本文档所述需求仅供个人学习研究与合法拥有的音乐内容个人备份使用。开发者应确保遵守相关法律法规及第三方应用服务协议，不得将本工具用于任何形式的版权侵权、商业分发或未经授权的数据采集。因不当使用产生的一切法律责任由使用者自行承担。